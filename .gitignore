.claude/

# Local environment files (including the repository-root Docker/Vite file).
# Examples stay tracked; real keys and deployment origins never do.
.env
.env.*
!.env.example

# Working notes / extracted proposal text (not source)
thesis_extract.txt
Final Thesis Proposal Paper_Update Carlo.pdf
THESIS_PAPER_REVISIONS_17_19.md
RAG_ITEMS_9_16_OLD_VS_NEW.md
THESIS_SYSTEM_CHANGE_PLAN.md
ISU_ECHAGUE_PRODUCTION_ROADMAP.md

# Superseded audit note. The current pair (PAPER_VS_SYSTEM_COMPARISON and
# SYSTEM_IMPROVEMENTS_AND_BUGS) is now tracked deliberately: the improvements
# inventory still lists open items with file:line pointers, and this repository
# is public, so treat closing those items as the priority they imply.
PRODUCTION_IMPROVEMENTS_RECOMMENDATIONS.md

# Duplicate local source asset; the application uses the optimized public copy.
ISU_Logo.jpg
/IsabelaLogo.jpg

# Local reindex checkpoint and generated email-template previews.
/rag-thesis-backend/.reindex_items_9_16_state.json
/rag-thesis-backend/email-templates/magic-link-preview.html
/rag-thesis-backend/email-templates/magic-link-preview.png
/rag-thesis-backend/email-templates/preview.html
/rag-thesis-backend/email-templates/reset-password-preview.html

# SonarQube scanner cache
.scannerwork/

# Local SQL dump files should not be committed. Database schema and numbered
# migrations are source code and must remain available to deployments.
*.sql
!rag-thesis-backend/supabase_setup.sql
!rag-thesis-backend/migrations/
!rag-thesis-backend/migrations/*.sql

# Local test and analysis artifacts
coverage.json
rag-thesis-frontend/coverage/
tmp/
*-trivy.txt
.pnpm-store/
tmp-pylint-cache/
tmp-pytest-*/
tmp-review-*/
# Loose scratch files, not just scratch directories: one-off rollback
# records written before a live data change (tmp-release-backup.json,
# tmp-authors-backup.json) sat untracked next to the source because every
# pattern above ends in a slash.
tmp-*.json
# ...and the verification logs that came with them: tmp-audit-*.log sat
# tracked at the repo root from 4847209 until 2026-09-14, referenced by
# nothing and carrying an absolute path from the machine that wrote them.
tmp-*.log
# One-off reproduction scripts. `_repro2_tmp.py` -- a script that provisions a
# throwaway Supabase admin user to reproduce an auth bug -- was committed on
# 2026-09-14 by a `git add -A` because every pattern above requires a leading
# "tmp". These name the machine's own paths and create real accounts; they are
# never part of the artifact.
_repro*
*_tmp.py
tmp_*.py
# Scratch *directories* beyond the three named above, which each spell out one
# tool. `rag-thesis-backend/tmp-probe/` was committed on 2026-09-14 by the same
# `git add -A` as `_repro*` and stayed tracked because no pattern here matches
# a bare `tmp-<word>/`. sonar-project.properties already generalises this as
# `rag-thesis-backend/tmp-*/**`; git did not, so the two configs disagreed
# about what counted as scratch.
tmp-*/
# Ad-hoc classifier probes at the backend root. `probe7b.py` was tracked in the
# same 2026-09-14 commit; it `exec()`s a file under /tmp that no clone has, so
# it cannot even be imported. It also escapes every gate -- outside pytest's
# `testpaths=tests`, outside pylint's explicit module list, outside `--cov`,
# and absent from the Dockerfile COPY list -- while still sitting inside
# `sonar.sources`, which analysed it as project code.
probe*.py
# Byte-code caches anywhere, not just under the backend. Running the
# out-of-tree pytest module in docs/evidence/security/audit-2026-09-05/ leaves
# a `__pycache__/` there, which rag-thesis-backend/.gitignore cannot reach.
__pycache__/
rag-thesis-backend/.venv*/
rag-thesis-backend/.pylint-cache-*/
rag-thesis-backend/.pytest-*/

# Supabase local backup
supabase/

# PI-08 controlled records. Never commit signatures, approval scans, or the
# identifiable working corpus register. Commit only templates and redacted
# hash receipts intended for the release evidence bundle.
docs/governance/private/
rag-thesis-backend/evaluation/corpus/private/
