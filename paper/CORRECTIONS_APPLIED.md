# Paper corrections applied - 2026-08-25

Source: `paper_ORIGINAL_2026-08-09.docx` (untouched backup of the Downloads file).
Output: `paper_CORRECTED.docx`. Regenerate with `python build_corrections.py` - it always
starts from the original, so it can never double-apply.

Verified after build: XML well-formed; `word/document.xml` and `word/media/image8.png`
are the only changed zip parts; the other 7 embedded images are byte-identical;
**0 paragraphs deleted**, 41 modified, 78 added. The proposal's future tense is
preserved throughout.

## Pass 1 - the 18 revisions from the paper-vs-system comparison

### Table cell values

| Table | Was | Now |
|---|---|---|
| T1 React | `v19.2.5` | `v19.2.8` |
| T1 Vite | `v8.0.8` | `v8.1.5` |
| T2 FastAPI | `v0.135.3` | `v0.139.2` |
| T2 Pydantic | `v2.12.5` | `v2.13.4` |
| T2 python-multipart | `v0.0.24` | `v0.0.32` |
| T3 supabase | `v2.28.3` | `v2.31.0` |
| T3 langchain-google-genai | `v4.2.1` | `v4.3.1` |
| T3 LLM name | `Gemini 1.5 Flash` | `Gemini 3.6 Flash` |
| T3 LLM id | `gemini-1.5-flash` | `gemini-3.6-flash` |
| T4 PyTest | `v8.1.1` | `v9.1.1` |
| T4 SonarQube | `v10.4` | `Community Build 26.7.0.124771` |
| T4 Pylint | `v3.1.0` | `v4.0.6` |
| T4 ESLint | `v9.0.0` | `v9.39.5` |
| T3 embedding | `text-embedding-004` | `Gemini Embedding` / `models/gemini-embedding-001 (768 dimensions)` |

### Rows added (26)

**Table 1 frontend** - `Node.js v24.18.0`, `Tailwind CSS v4.3.3`, `React Router v8.3.0`, `TanStack Query v5.101.4`, `Axios v1.18.1`, `Framer Motion v12.42.2`

**Table 2 backend** - `Python v3.14.7`, `pydantic-settings v2.14.2`, `Uvicorn v0.51.0`, `SlowAPI v0.1.10`, `Redis v8.0.1`, `cryptography v50.0.0`, `PyJWT v2.13.0`, `HTTPX v0.28.1`

**Table 3 AI/RAG** - `Gemini 3.5 Flash Lite gemini-3.5-flash-lite`, `langchain-core v1.5.1`, `langchain-text-splitters v1.1.2`, `tiktoken v0.13.0`, `PyMuPDF v1.28.0`, `tesserocr v2.10.0`, `Pillow v12.3.0`, `LangSmith v0.10.10`

**Table 4 testing** - `Node.js test runner v24.18.0 (node --test)`, `Playwright v1.61.1`, `axe-core for Playwright v4.12.1`, `SonarScanner CLI v8.0.1.6346`

### Claims corrected

- **embed prose A** - was: _such as Gemini's text-embedding-004_
- **embed prose B** - was: _the Gemini text-embedding-004 model_
- **embed prose C** - was: _powered by Gemini's text-embedding-004_
- **T6 embed** - was: _Gemini text-embedding-004, Supabase pgvector_
- **LLM prose** - was: _via the Gemini 1.5 Flash model_
- **document loaders** - was: _LangChain's document loaders to process digitized CCSICT theses_
- **RetrievalQA #1** - was: _LangChain's 'RetrievalQA' chains will be engineered_
- **RetrievalQA #2** - was: _utilizing LangChain's RetrievalQA_
- **LongContextReorder** - was: _a context reordering algorithm, such as LangChain's LongContextReorder_
- **T6 Obj3 versions** - was: _React v19.2.5, Vite v8.0.8, FastAPI v0.135.3_
- **T6 Obj2 metrics** - was: _Empirical performance data on Faithfulness and Context Precision_
- **P1-5 metric semantics** - was: _calculating automated reference-free scores for Faithfulness and Context Precision._
- **P1-5 inferential metric** - was: _generated by the Ragas framework, particularly in terms of Faithfulness and Context Precision._
- **P1-6 duplication numbers** - was: _the model also generates a brief summary of the matched archival study._
- **P2-10 digitization boundary** - was: _conversion of physical hardbound copies and unstructured digital softcopies from the CCSICT archives..._
- **P2-10 single OCR engine** - was: _(Tesseract OCR or EasyOCR)_
- **P2-17 thesis categories** - was: _The vector database is strictly limited to undergraduate theses generated within the CCSICT departme..._
- **P2-7 tokenizer caveat** - was: _configured to an empirically optimized 800-token chunk size with a 100-token overlap to preserve con..._
- **P2-8 retrieval constants** - was: _the pipeline will enforce a minimum cosine similarity threshold;_
- **P2-9 actor model** - was: _which will accommodate distinct user roles._
- **P2-12 hardware framing** - was: _To develop and test the system effectively, the researchers will use a laptop with specific hardware..._
- **P3-14 PII softening** - was: _by ensuring that no personally identifiable information (PII)_
- **P1-1 release fingerprint** - was: _retrieve highly similar vector embeddings from the Supabase database before synthesizing a response._
- **P3-15 citation limitation** - was: _produce highly traceable, accurate in-line citations._
- **P3-16 production surround** - was: _As illustrated in Figure 8, the system architecture will be structured into four primary interactive..._

## Pass 2 - full sweep of all 427 non-empty paragraphs

Pass 1 followed the comparison report's revision list. Reading the whole paper
afterwards showed that **six of those revisions name more than one section, and only
the first had been edited** - and turned up three inaccuracies the comparison never
recorded. All eight fixed:

- **A1/A3 3.1.3 corpus + boundary** - was: _The data will only include past undergraduate thesis projects from the department, using both physic..._
- **A2 3.2.1 category sentence** - was: _This approach ensures that the evaluation covers diverse technical vocabularies relevant to the doma..._
- **A4 Phase 1 boundary** - was: _Physical, hardbound thesis manuscripts from the CCSICT archives will be converted into machine-reada..._
- **A5 Phase 3 duplication numbers** - was: _This triggers an automated warning for the AI model, ensuring that any potential duplicates are clea..._
- **A6 3.3 retrieval constants** - was: _which will search the Vector Database for the most semantically relevant thesis chunks._
- **B1 1.5 definition** - was: _indirect access to undergraduate thesis knowledge_
- **B2 analytics is admin-only** - was: _a cohesive platform where students and faculty can execute literature reviews, conduct novelty scann..._
- **B4 four approval gates** - was: _First, the researchers will obtain formal written approval from the CCSICT Department Chair and the ..._

## Pass 3 - track vocabulary, resolved

The department ruled the academic catalog authoritative. CCSICT has five programs but
only three specializations; `services/catalog.py:110` falls back to the program **code**
when a program takes none, and `SPECIALIZATION_REQUIRED_PROGRAMS = {'BSCS','BSIT'}`
means those two can never be a track themselves. The legal `papers.track` values are:

```
Data Mining | Web and Mobile Application Development | Network and Security
BSDSA | BSIS | BLIS
```

- **Track vocabulary 3.2.1** - was: _ensuring representation across various academic tracks such as Data Mining, Web Development, and Net..._

Phrasing mirrors `golden_dataset.json`, realigned in `13f10c6`, so the paper and the
dataset cannot drift apart. All five legacy track strings are now absent from the paper;
the two surviving "Data Mining" mentions are the authors' own degree track on the title
page and the methodological framing in 2.1.8, both correct as written.

## Still open - needs you

1. ~~**Figure 1 must be redrawn.**~~ **Closed 2026-08-30 (Pass 5 below).** Figure 1 is
   now generated and applied by the build, exactly like Figure 8.

2. **Figures 3, 4 and 5 carry internal titles** reading "Figure 1.1", "Figure 1.2" and
   "Figure 1.3" while the paper captions them Figure 3, 4 and 5. Cosmetic.
3. **`pgvector v0.7.0`** in Table 3 is unverified - Supabase-managed and not recorded in
   the repository. Confirm in the dashboard.
4. **Row order in Table 2.** `Python v3.14.7` was appended and sits after `python-dotenv`.
   One drag in Word.
5. **Chapter 4 does not exist yet**, so the reporting-discipline revision applies when you
   write it.

Not verifiable from the repository, left as written: the Gemini API no-training
deployment terms (2.1.6) and the per-thesis size estimates (3.1.3).

**The file has not been opened in Word.** Verification here is structural.

## Pass 4 - Figure 8 replaced, 2026-08-30

The architecture figure is now generated rather than hand-drawn, and the swap is part of
`build_corrections.py`, so a rebuild can no longer restore the stale image.

- Source: `figures/figure8_system_architecture.png`, generated by
  `Figure8_System_Architecture.build.py` (hand-laid-out SVG, rasterized at 3360x2860).
- Applied by `apply_figures()`, which also refits the drawing. The original was 7.67 in
  wide on an A4 page with 1 in margins - **1.4 in past the right margin**. It is now
  fitted to the measured text width: 6.27 x 5.34 in.

What the original figure claimed, and what the replacement shows:

| Original | Replacement |
|---|---|
| `gemini-1.5-flash` | `gemini-3.6-flash` |
| `text-embedding-004` | `gemini-embedding-001`, 768 dimensions |
| RAG Pipeline "LangChain & langchain-community" | LangChain Expression Language chain |
| Actors: Admin, Student, Faculty, **Researcher** | Guest Researcher, Student, Faculty, Admin, **Superadmin** |
| Synchronous upload into the Embedding Layer | Upload API stages and queues (HTTP 202); a leased **Ingestion Worker** commits atomically |

Also now shown, and absent from the original: the Duplication Guard at 0.85 on both the
ingest and query paths, the Citation Validator, PII redaction, `upload_jobs` lease and
idempotency, and the retrieval constants (cosine 0.30, top-k 5, department-scoped).

> **Resolved 2026-08-30 — the embedding model is `gemini-embedding-001`.** The
> researchers confirmed the deployed model. Table 3, Table 6, and the three "embed
> prose" corrections now read **Gemini Embedding** with the id
> `models/gemini-embedding-001 (768 dimensions)`, matching Figure 8 and
> `rag-thesis-backend/config.py:18`. No occurrence of `Gemini Embedding 2` or
> `gemini-embedding-2` remains in the paper.
>
> The dated evidence under `docs/evidence/` still records `gemini-embedding-2`, because
> that is what those runs actually used. Those artifacts are historical records and are
> deliberately left unaltered; the release smoke needs re-running against
> `gemini-embedding-001` before the final defense.
>
> **Done 2026-09-03.** `docs/evidence/security/pi-03-gemini-20260903-045753/` records a
> PASS of `scripts/gemini_release_smoke.py` inside the release image built from `9cb3659`,
> on the direct Google route: `gemini-3.6-flash` and `gemini-3.5-flash-lite` answered,
> and `gemini-embedding-001` returned 768 finite values. The 2026-07-25 bundle is
> superseded, not altered.

## Pass 5 - Figure 1 replaced, 2026-08-30

The remaining stale figure is now generated rather than hand-drawn, closing open item 1
above. Both figures are applied by the same `apply_figures()` table in
`build_corrections.py:347-348`, so a rebuild can never restore either stale image.

- Source: `figures/figure1_rag_architecture.png`, generated by
  `Figure1_RAG_Architecture.build.py` (hand-laid-out SVG, committed alongside as
  `Figure1_RAG_Architecture.svg`).
- It is a faithful redraw of the original design - the seven steps, their wording, the
  two-row wrap and the colour treatment are unchanged. Only the two model labels move:

| Original | Replacement |
|---|---|
| `text-embedding-004` | `gemini-embedding-001` |
| Gemini 1.5 Flash | Gemini 3.6 Flash |

Verified after the build: `word/media/image1.png` inside `paper_CORRECTED.docx` is
byte-identical to `figures/figure1_rag_architecture.png`
(SHA-256 `b8ba11f6e96e38a4...`, 190,123 bytes), and `word/media/image8.png` likewise
matches `figures/figure8_system_architecture.png`
(SHA-256 `14a77e6796eb338c...`, 373,437 bytes). No occurrence of `text-embedding-004`
or `gemini-1.5-flash` now survives in either the body text or the embedded figures.

## Pass 6 - Table 3 dependency drift, 2026-09-01

`paper_CORRECTED.docx` was built on 2026-08-30 from commit `2b29f9a`. Three feature
commits landed after it, so the version tables had drifted out of date for the first
time since they were generated:

| Commit | Date | Effect on the paper |
|---|---|---|
| `9dc343c` | 2026-08-31 | Reserve Gemini key pool. No new dependency; no table change. |
| `a50b59b` / `7852349` | 2026-08-31 / 09-01 | Optional OpenAI-compatible chat gateway. Adds `langchain-openai==1.4.1` to the production set. |
| `4d216ea` | 2026-09-01 | Audit fixes. No dependency change. |

Applied:

- **Table 3** gains `langchain-openai v1.4.1`, matching `requirements.txt:18`. Every
  other cell in Tables 1-4 was re-checked against `package.json` and
  `requirements.txt` and needed no change.
- **2.1.6 and 3.2.6** now state the boundary of the no-training guarantee. The paper
  rests its data-governance argument on Google's Gemini API terms; when `LLM_BASE_URL`
  is set, chat, extract and verdict calls are served by a third party those terms do
  not cover. Embeddings are never routed, so every question still reaches Google.
  `scripts/release_fingerprint.py` now records `generation_route` (enabled flag and
  gateway host, never the credential) so a reported result cannot silently come from a
  different provider than the one described.

## Pass 7 - accuracy and citation corrections, 2026-09-03

Applied in commits `a1893b1` and `e717acb` through `PROSE7` in `build_corrections.py`,
and reached `paper_CORRECTED.docx` and `.pdf` on 2026-09-03; this log had not recorded
them until the same-day review below found the gap.

- **P7-1 SonarQube memory leaks** - was: _logic flaws, unhandled exceptions, memory
  leaks, and potential fault points_. SonarQube's Python and JavaScript analysers do not
  detect memory leaks; the phrase is dropped.
- **P7-2 Pylint profile** - 3.2.4 now states that scores are reported against the
  committed `.pylintrc`: 120-character lines, the test suite excluded, and fourteen
  message families disabled including the docstring and function-size checks.
- **P7-3 Li et al.** - was: _literature (Li, 2025;_ in 2.1.7. APA 7 cites three or more
  authors as "et al." at every mention.
- **P7-4 same-family judge** - 3.2.4 now discloses that the Ragas judge is Gemini 3.5
  Flash-Lite scoring Gemini 3.6 Flash answers, with Answer Correctness embedded by
  `models/gemini-embedding-001`, the retriever's own model.
- **P7-5 notices and effect size** - 3.2.5 now states that the RAG diagnostics are
  reported for answers and for notices separately as well as pooled, and that every
  comparison carries the mean paired difference with a 95% confidence interval and a
  standardized effect size.

## Pass 8 - dependency purposes and metric semantics, 2026-09-03

Found by re-reading Tables 2-3 and 3.2.4 against the code rather than against the
dependency lists.

| Where | Was | Now | Ground truth |
|---|---|---|---|
| Table 3, Gemini 3.5 Flash Lite | handles bounded verdict **and metadata-extraction** work | writes novelty-scan verdict summaries and follow-ups and serves as the Ragas judge; title-page metadata extraction runs on the primary generation model | `services/gemini_pool.py` builds the EXTRACT client from `gemini_chat_model`; only VERDICT uses `gemini_verdict_model` (`routers/upload.py:620`, `routers/duplication.py:333`, `evaluation/run_comparison.py`) |
| Table 2, cryptography | primitives for token and secret handling | encrypts the storage backups (AES-GCM under a scrypt-derived key); pinned at v50.0.0 for CVE-2026-69247 | the only first-party import is `scripts/storage_backup.py:16-34`; the pin reason is in `requirements.txt` |
| Table 2, PyJWT | encode and verify session JWTs | verify and read Supabase-issued JWTs: the assurance level for the privileged MFA check and the user id for per-user rate limits; the backend never issues a token | `dependencies/auth.py:177-213`, `services/rate_limiting.py:18`; no `jwt.encode` in the codebase |
| Table 2, Redis | "Redis v8.0.1" as if it were the server | "redis (Python client) v8.0.1", naming the redis 8.6.4 server from the operations compose file | `requirements.txt` pins the client; `docker-compose.operations.yml` runs the server |
| 3.2.4 metric semantics | _These Ragas metrics are reference-based rather than reference-free_ | Answer Correctness and Context Precision are reference-based; Faithfulness is reference-free and scored against the retrieved passages alone | ragas 0.4.3: `Faithfulness.ascore(user_input, response, retrieved_contexts)` takes no reference; `ContextPrecision` is the with-reference wrapper; `run_comparison.py` calls them exactly so |

The same extraction-model error was in `README.md` (model defaults bullet) and is fixed
there in the same change.

Verified after build: `word/document.xml`, `word/media/image1.png` and
`word/media/image8.png` are the only changed zip parts; XML well-formed; the original's
350 non-empty paragraphs become 431, of which 45 originals are reworded. No paragraph
is deleted: the build only replaces runs, rewrites cells, and appends table rows. The
PDF was re-exported with Word 2024 and is now 62 pages (the longer Table 2 and Table 3
cells push one page); PyMuPDF extracts 86,389 characters, containing the Pass 8 wording
and none of the superseded strings (`text-embedding-004`, `gemini-1.5-flash`,
`memory leaks`, `metadata-extraction work`, `token and secret handling`).
