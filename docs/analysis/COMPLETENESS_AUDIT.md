# Completeness audit

Four critics, each given one lens and run after every subsystem map and flow trace had been fact-checked. Their job was to find what the analysis missed. Reproduced in full; entry point: [`../CODEBASE_ANALYSIS.md`](../CODEBASE_ANALYSIS.md).

The second lens (user-facing features) is also summarized in section 10 of the entry point.

1. [Completeness audit: tracked (and untracked) source files with real logic that appear in NO subsystem map's key...](#c1) - 35 findings
2. [User-facing features and capabilities present in the code but absent or under-described in the subsystem maps ...](#c2) - 18 findings
3. [Contracts, invariants and coupling points: frozen constants and version strings, exact literals matched across...](#c3) - 17 findings
4. [Genuine correctness, consistency and security risks not surfaced by the subsystem maps or flow traces: order-d...](#c4) - 13 findings

---

## C1

<a id="c1"></a>

**Lens.** Completeness audit: tracked (and untracked) source files with real logic that appear in NO subsystem map's keyFiles list. Method: `git ls-files` (511 tracked) sorted and `comm`-diffed against the union of every keyFiles array in the digest (335 unique paths, 7 of which are untracked-but-covered). Result: 173 tracked files uncovered, of which 71 carry real logic (9 .py, 18 .js, 20 .jsx, 1 .mjs, 17 .sql, 6 .html); the rest are PNG/JPG/SVG assets, .jtl/.json evidence, SHA256SUMS receipts, lockfiles and .gitignore files. Untracked working tree checked with `git status --porcelain --untracked-files=all`: 3 files uncovered, 7 already in keyFiles. One keyFile in the digest (`supabase/.temp/`) is not a tracked file at all.

**35 findings.**

- **17 of the 27 files in rag-thesis-backend/migrations/ are in no keyFiles list. The DB-schema map named 9 migrations + supabase_setup.sql; the directory has 27 .sql files. The uncovered ten forward migrations are listed in the following entries, plus seven .rollback.sql files.**
  - Because these are `create or replace function`, an uncovered migration can be the AUTHORITATIVE body of a function the maps attribute to supabase_setup.sql. Three of them are exactly that (see the next three entries).
  - `rag-thesis-backend/migrations/ (27 .sql files; covered: 20260717_rag_items_9_16, 20260718_transactional_ingestion_cleanup, 20260719_production_hardening, 20260720_index_embedding_provenance, 20260723_durable_ingestion_jobs, 20260724_operations_security, 20260725_normalized_academic_catalog, 20260804_backup_runs, 20260819_thesis_category, 20260903_hnsw_ef_search)`

- **20260724_operations_security_fixes.sql (54 lines) — re-creates `upsert_operational_alert` so acknowledgement survives while an alert is still active and `occurrence_count` increments only on a genuine reopen. The digest's DB map already cites this file as the authoritative body at line :7, but the file itself is in no keyFiles list.**
  - `rag-thesis-backend/migrations/20260724_operations_security_fixes.sql:7`

- **20260829_reindex_updates_chunk_count.sql (45 lines) — the authoritative `activate_paper_index` body; adds the `papers.chunk_count = <counted value>` update that the 20260717 and 20260720 bodies lack. Cited in the DB map's surface as authoritative, absent from every keyFiles list.**
  - `rag-thesis-backend/migrations/20260829_reindex_updates_chunk_count.sql:4`

- **20260828_allow_any_email_signup.sql (39 lines) — the authoritative `handle_new_user()` trigger body: role='faculty' only when `raw_user_meta_data->>'requested_role'='faculty'`, department hard-coded 'CCSICT', status 'pending' for faculty else 'approved'. This is the account-default policy for the whole system, and public sign-up never touches FastAPI, so this file IS the authorization default.**
  - `rag-thesis-backend/migrations/20260828_allow_any_email_signup.sql:10`

- **20260803_institutional_signup_domain.sql (65 lines) — the SUPERSEDED predecessor of the above: it held any non-'@isu.edu.ph' account at status='pending'. 20260828 reverses that policy. Both are committed, apply in filename order, and neither is in any map — so the 'why did open registration become allowed' history is dark.**
  - `rag-thesis-backend/migrations/20260803_institutional_signup_domain.sql`

- **20260804_chat_message_kind.sql (120 lines) — adds `chat_messages.kind` with CHECK (answer|notice), drops the 8-arg `save_chat_exchange` and re-creates it with `p_kind`, and backfills historical notice rows by text prefix. Referenced by name in tests/test_chat_notices.py's own keyFiles bullet but never as a subsystem keyFile.**
  - `rag-thesis-backend/migrations/20260804_chat_message_kind.sql`

- **20260825_notice_kind_greeting_and_fallback.sql (55 lines) — extends that backfill to the two notices 20260804 missed: the greeting/identity reply and the grounded-retrieval fallback, both of which were being persisted as research answers and replayed to the model as context.**
  - `rag-thesis-backend/migrations/20260825_notice_kind_greeting_and_fallback.sql`

- **20260819_isu_academic_catalog.sql (134 lines) — seeds all ten ISU Echague colleges plus 32 standby programs and 9 majors; CCSICT is the only `departments.active = true` row, and that single flag is what makes `routers/catalog.py::_nested_catalog` hide the rest and `resolve_academic_selection` 422 an upload addressed to them. The DB map reports the seeded rows but not the file that seeds them.**
  - `rag-thesis-backend/migrations/20260819_isu_academic_catalog.sql`

- **20260717_security_scope_evaluation.sql (118 lines) — the earliest `handle_new_user()` body plus the strict indirect-access/ingestion-state hardening. First link in the three-file trigger chain.**
  - `rag-thesis-backend/migrations/20260717_security_scope_evaluation.sql:4`

- **20260720_backfill_missing_profiles.sql (25 lines) — one-shot idempotent backfill inserting `profiles` rows for auth.users accounts created before the trigger existed, defaulting full_name to the email local-part then 'ISU User'.**
  - `rag-thesis-backend/migrations/20260720_backfill_missing_profiles.sql`

- **20260803_upload_jobs_owner_on_delete_set_null.sql (29 lines) — drops NOT NULL on `upload_jobs.owner_id` and switches the FK to ON DELETE SET NULL so deleting an uploader no longer aborts with an opaque 500 while the ingestion audit trail survives. This is the file behind the DELETE /analytics/users 23503→409 path.**
  - `rag-thesis-backend/migrations/20260803_upload_jobs_owner_on_delete_set_null.sql`

- **Seven .rollback.sql files, none in any keyFiles list: 20260725_normalized_academic_catalog.rollback.sql (20 lines), 20260804_backup_runs.rollback.sql (12), 20260804_chat_message_kind.rollback.sql (65, restores the 8-arg save_chat_exchange and drops `kind`), 20260819_isu_academic_catalog.rollback.sql (30), 20260819_thesis_category.rollback.sql (119, drops and re-creates the SIX-argument match_chunks exactly as 20260720 defined it), 20260825_notice_kind_greeting_and_fallback.rollback.sql (30), 20260903_hnsw_ef_search.rollback.sql (23).**
  - tests/test_schema_consistency.py explicitly SKIPS .rollback. files when building its drift map, so these 299 lines are the only committed SQL under no consistency gate — and 20260819_thesis_category.rollback.sql:11 contains a second, non-authoritative `create or replace function public.match_chunks` that a mis-ordered replay would install.
  - `rag-thesis-backend/migrations/*.rollback.sql`

- **docs/deck/pptx_helpers.py (317 lines, 25 public functions + `Overflow`) — the entire python-pptx drawing layer for the defense deck: `blank_slide`, `glass_card`, `rect`, `circle`, `text` (raises Overflow rather than overlapping a forced box), `picture`/`flat_picture`, `chip`, `block_arc`, `hline`, `set_fill`/`set_line`/`add_shadow`/`set_picture_opacity` (raw DrawingML appended after python-pptx styling), `set_name` (load-bearing for Morph matching), `set_notes`. Imported by docs/deck/build.py:33,63,673, build_defended_slide.py and measure.py.**
  - The documentation map lists build.py, charts.py, theme.py, xml_post.py, measure.py, fonts.py and orb.py but not the module all of them draw through. Every 'no grouped shapes / every run gets an explicit size' invariant the deck verifier checks is enforced here, not in build.py.
  - `docs/deck/pptx_helpers.py`

- **paper/Figure8_System_Architecture.build.py (259 lines) — emits Figure8_System_Architecture.svg with explicit hand-set coordinates (W,H = 1680x1430) because auto-layout routes the long ingestion edges through the retrieval column; its output PNG is one of the two images build_corrections.py swaps into the corrected .docx.**
  - `paper/Figure8_System_Architecture.build.py`

- **paper/Figure1_RAG_Architecture.build.py (151 lines) — emits Figure1_RAG_Architecture.svg, a faithful redraw of the original figure changing only the two stale model labels ('text-embedding-004'→'gemini-embedding-001', 'Gemini 1.5 Flash'→'Gemini 3.6 Flash'). This is where the frozen model names reach the paper's figures.**
  - The 'Frozen contracts' rule says model names appear in the paper's tables via build_corrections.py; they also appear in these two generated figures, and neither builder is run by build_corrections.py nor pinned by any test.
  - `paper/Figure1_RAG_Architecture.build.py:6-9`

- **docs/evidence/security/audit-2026-09-05/test_audit_reproductions.py (323 lines, 14 pytest cases for findings F02-F12) — a second, deliberately out-of-tree pytest module that imports `tests.conftest` from ../../../../rag-thesis-backend and drives FastAPI TestClient to DEMONSTRATE current defects (oversized-file spooling on 3 upload routes, guest-id rate-limit reset, faculty aal1 novelty bypass, chat-edit branch loss, unbilled guest follow-up generation, deterministic citation repair attaching an unrelated source, CORS PATCH refusal).**
  - Passing means the defect reproduced, not that it is fixed — and because it lives outside rag-thesis-backend/tests it is invisible to pytest.ini collection, the --cov-fail-under=85 gate and CI. Its 14 recorded results are in reproductions.xml (also uncovered). An untracked __pycache__/*.pyc shows it was executed on this checkout.
  - `docs/evidence/security/audit-2026-09-05/test_audit_reproductions.py`

- **docs/evidence/security/audit-2026-09-05/audit_browser.mjs (99 lines) — Playwright harness for finding F01: boots vite `preview` on the existing dist-e2e build at 127.0.0.1:4174, seeds two synthetic `isu_e2e_auth_fixture` identities (Alice/Bob), switches accounts inside the SPA and asserts Alice's scan-history row is still rendered to Bob from React Query's cache. Writes browser-reproduction.json and cross-account-cache.png.**
  - It reaches into rag-thesis-frontend/node_modules/vite and @playwright/test by relative URL and hard-codes port 4174 to avoid the E2E harness's strictPort 4173 — an undocumented second consumer of the e2e build that no frontend map mentions.
  - `docs/evidence/security/audit-2026-09-05/audit_browser.mjs`

- **rag-thesis-backend/tmp-probe/probe.py (35 lines) — a tracked scratch script: sets 14 fake env vars inline, calls silence_known_third_party_warnings(), then reads a question file from argv[1] and prints which private chat classifiers fire per line (_is_capability_question, _is_identity_question, _is_simple_conversation, _is_system_origin_question, _is_ambiguous_system_origin_question, _is_self_platform_reference, _is_model_question, _is_courtesy_message, _is_archive_inventory_question) plus guards.prohibited_reason.**
  - CLAUDE.md says `tmp*/` is local scratch and gitignored. It is not: .gitignore has `tmp/`, `tmp-pylint-cache/`, `tmp-pytest-*/`, `tmp-review-*/` but nothing matching `tmp-probe/`, `git check-ignore` exits 1, and the file was committed in 576f59f alongside real features. It duplicates the already-covered rag-thesis-backend/probe7b.py (45 lines).
  - `rag-thesis-backend/tmp-probe/probe.py`

- **Four package markers, none in any keyFiles list: rag-thesis-backend/evaluation/__init__.py ('Objective 2 evaluation harness: baseline LLM vs RAG + LLM comparison.'), scripts/__init__.py ('Operator-only maintenance commands.'), workers/__init__.py ('Standalone backend worker processes.'), tests/__init__.py (empty, 0 bytes).**
  - They are why `python -m scripts.<name>` and `python -m evaluation.run_comparison` resolve at all, and tests/__init__.py being a real package is what lets the out-of-tree audit module do `import tests.conftest`.
  - `rag-thesis-backend/{evaluation,scripts,workers,tests}/__init__.py`

- **All 18 frontend unit-test files that actually run under `npm test` are absent from the frontend test-harness map's keyFiles (it listed only the 4 in src/design/ and src/components/three/): src/design/contrast.test.js (3), src/lib/apiErrors.test.js (6), catalog.test.js (10), idleSession.test.js (7), keys.test.js (5), pagination.test.js (5), permissions.test.js (5), privilegedMfa.test.js (15), utils.test.js (22), src/pages/archive/archiveCatalog.test.js (8), src/pages/auth/authUtils.test.js (4), src/pages/chat/messageNotice.test.js (11), responseActions.test.js (4), transcript.test.js (10), src/pages/novelty/report.test.js (2), src/pages/upload/batchState.test.js (23), uploadState.test.js (8), wizardSteps.test.js (14).**
  - These are the only enforcement of the CI gate (lines 85 / functions 85 / branches 80). The largest, batchState.test.js at 470 lines / 23 tests, is the sole guard on the 19-action batchReducer and on restoreActiveBatch's owner-mismatch rejection — which is the client-side half of the cross-account leak that audit_browser.mjs reproduces.
  - `rag-thesis-frontend/src/**/*.test.js (22 files total; 18 uncovered)`

- **20 frontend .jsx components appear in the design-system map's `surface` prop signatures but in no keyFiles list, so nothing anchored them to a file: components/AppearanceDialog.jsx (27), components/CommandPalette.jsx (98), three/AuthScene.jsx (38), three/HeroScene.jsx (57), three/ParticleField.jsx (100), three/ThesisCards.jsx (78), ui/AnimatedLogo.jsx (81), ui/Aurora.jsx (41), ui/Badge.jsx (28, also exports RoleBadge), ui/EmptyState.jsx (23), ui/GlassCard.jsx (29), ui/Logo.jsx (35, also BrandMark), ui/LogoActivityDots.jsx (36), ui/ProgressRing.jsx (51), ui/Sheet.jsx (59), ui/Skeleton.jsx (12), ui/Spinner.jsx (21), ui/TableScroller.jsx (23), ui/TableStateRow.jsx (75), ui/Tooltip.jsx (35).**
  - 947 lines with zero unit tests and no keyFile anchor. The four three/ scenes are the payload behind the useSceneRuntime lazy-chunk budget (250 kB) that check-bundle-budget.mjs enforces, and ui/Badge.jsx is the sole consumer of the badgeTones contract that IS tested.
  - `rag-thesis-frontend/src/components/{AppearanceDialog,CommandPalette}.jsx, src/components/three/{AuthScene,HeroScene,ParticleField,ThesisCards}.jsx, src/components/ui/ (14 files)`

- **Three Supabase Auth email templates (Go `{{ .ConfirmationURL }}` / `{{ .Token }}` / `{{ .Email }}` / `{{ .SiteURL }}` placeholders) plus their two runbooks: confirm-signup.html (197), magic-link.html (197), reset-password.html (202), README.md (131), logo-host.md (76).**
  - CLAUDE.md names the directory but no subsystem map lists any file in it, so the OTP/magic-link/recovery email surface the Login.jsx verifyOtp flow depends on is undescribed. logo-host.md documents that the templates reference `{{ .SiteURL }}/isu-thesis-ai-mark.png`, which is the ONLY reference to public/isu-thesis-ai-mark.png anywhere — grep finds no src/ or index.html use. Three sibling *-preview.html files and magic-link-preview.png exist on disk but are gitignored (.gitignore:29,31).
  - `rag-thesis-backend/email-templates/`

- **Frontend build/config files in no keyFiles list: index.html (24 lines — the Vite entry; preconnects fonts.googleapis.com/fonts.gstatic.com, `<meta name="theme-color" content="#04160c">`, loads /src/main.jsx), components.json (shadcn 'new-york' style, tsx:false, baseColor neutral, cssVariables, lucide icons, @/ aliases), .nvmrc (24.18.0), .env.example (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY / VITE_API_URL / VITE_TURNSTILE_SITE_KEY), README.md (89 lines, pinned by tests/test_readme_accuracy.py:148-174).**
  - index.html is where the Google Fonts origins enter the page, and nginx/default.conf.template's CSP (which IS covered) has to allow exactly those two hosts — the pair is only checkable with both files in view.
  - `rag-thesis-frontend/{index.html,components.json,.nvmrc,.env.example,README.md}`

- **Five tracked front-end binary/vector assets with ZERO references in src/, index.html or nginx/: public/icons.svg (a 7-symbol sprite: bluesky-icon, discord-icon, documentation-icon, github-icon, social-icon, x-icon), public/isu-thesis-ai-mark.png (used only by the email templates via {{ .SiteURL }}), src/assets/hero.png, src/assets/react.svg, src/assets/vite.svg (Vite scaffold leftovers).**
  - Dead weight in the repo, though not in the bundle — Vite only emits src/assets files that are imported, so these do not count against the 330 kB eager budget. icons.svg's social-network symbols suggest a landing footer variant that no longer exists.
  - `rag-thesis-frontend/public/{icons.svg,isu-thesis-ai-mark.png}, rag-thesis-frontend/src/assets/{hero.png,react.svg,vite.svg}`

- **docs/evidence/contracts/iskai-openapi.current.json (100,069 bytes, 49 paths, info.version '2.1.0') and iskai-openapi-2026-07-25.json (74,837 bytes).**
  - tests/test_export_openapi.py:27-45 asserts BYTE equality between `scripts.export_openapi.render_openapi()` and iskai-openapi.current.json, so this file is a frozen contract on the same footing as PROMPT_VERSION — adding, renaming or re-annotating any route fails CI until it is regenerated. It appears in the test-suite map's surface as a path but in no keyFiles list, and the 2026-07-25 snapshot (the diff baseline) is unmentioned anywhere.
  - `docs/evidence/contracts/`

- **rag-thesis-backend/evaluation/corpus/corpus_manifest.template.json — the committed PI-08 template: corpus_id 'ISU-ECHAGUE-CCSICT-DEFENSE-2026', expected_paper_count 12, four null approval slots (ccsict_department_chair, privacy_officer, thesis_adviser, university_librarian), processing_profile {allowed_content: 'redacted_non_personal_non_confidential_only', gemini_service_tier: 'unpaid'}, and released_composition declaring represented BSCS/Data Mining 5 + BSIS 3 + BLIS 4 = 12 against an explicit `unrepresented` list.**
  - This is the actual input schema that scripts/corpus_manifest.py validates and locks, and the only committed statement of the census-not-sample sampling argument. The CLI map covers the script; nothing covers the document it validates.
  - `rag-thesis-backend/evaluation/corpus/corpus_manifest.template.json`

- **rag-thesis-backend/evaluation/results/comparison_20260728_140718.json — an EARLIER, superseded Objective 2 run with `formal_result: false`, no `queries_total`, no `by_corpus_coverage` and no `unattempted_query_ids` (12 top-level keys vs the 20-key contract the current run writes).**
  - Two comparison_*.json files sit in the same directory and only the 2026-09-13 one is pinned (by docs/deck/evidence.py RESULTS_JSON and scripts/export_objective2_summary.py SOURCE). A glob-based reader would pick up the 2026-07-28 file, which predates the result-JSON contract. Also uncovered: comparison_20260913_064053.csv, the flat sibling of the covered .json.
  - `rag-thesis-backend/evaluation/results/comparison_20260728_140718.json`

- **JMeter chat-load raw results and their derived summaries, none in any keyFiles list: chat-2.jtl, chat-5.jtl, chat-10.jtl, chat-20.jtl (the four profiles summarize_chat_load.py keys as `<stem minus 'chat-'>_users`), chat_summary_{2u,5u,10u,20u}.json, chat_rag_load_report.json (schema_version 1, corpus 'synthetic-10-thesis-80-chunk', provider_tier 'gemini-free', and the recorded 100 ms / 2000 ms classification basis), and chat-probe.xml — the companion run that captured response BODIES and is the sole evidence that every sub-second HTTP 200 carried CAPACITY_MESSAGE.**
  - chat-probe.xml is the only artifact that justifies summarize_chat_load.py's CAPACITY_MAX_MS=100 / ANSWER_MIN_MS=2000 split — its first sample is a 1509 ms HTTP 200 whose body is the capacity notice, which incidentally contradicts the 100 ms rule it is cited to support. 11 further .jtl/.json files for the gemini/provider/rate profiles sit in the same directory untracked-and-gitignored, so the committed set is partial.
  - `rag-thesis-backend/evaluation/results/jmeter/`

- **Two standalone HTML evidence reports: docs/FIDELITY_AUDIT_2026-09-02.html (412 lines, 56 kB, title 'IskAI Fidelity Audit', no scripts) and docs/evidence/OBJECTIVE_2_COMPARISON_2026-09-13.html (968 lines, 49 kB, title 'CCSICT Baseline vs RAG', TWO inline <script> blocks).**
  - A third, undescribed rendering path for the same Objective 2 figures that already flow to evaluation/results/*.json, src/data/objective2Summary.json and the PPTX deck — with its own inline JS and no generator script in the repo, so it cannot be regenerated and cannot be checked against the run it quotes.
  - `docs/FIDELITY_AUDIT_2026-09-02.html, docs/evidence/OBJECTIVE_2_COMPARISON_2026-09-13.html`

- **docs/deck/README.md (145 lines — the deck's reproducibility runbook and the 11-slide order: title, background, objectives, obj1, refusal guard, obj2, obj3, architecture, obj4, Q&A, closing) and docs/deck/requirements-deck.txt (python-pptx==1.0.2, lxml==6.1.3, matplotlib==3.10.8, numpy==2.5.3, pillow==12.1.0).**
  - requirements-deck.txt is a fifth pinned dependency set (after requirements.txt, requirements.lock, evaluation/requirements-eval.txt and package.json) and the only one under no test: tests/test_dependency_lock.py reads requirements.txt/.lock only, and tests/test_readme_accuracy.py does not read docs/deck/README.md.
  - `docs/deck/{README.md,requirements-deck.txt}`

- **docs/deck/assets/captures/archive_state.json — the archive counters the deck quotes as of the capture run: total_papers 16, total_tracks 4, year_range 2024-2026, total_queries 197, read_on 2026-09-14, source '/analytics/summary'.**
  - manifest.json is a keyFile but its sibling is not, and this file is the only record of what the live archive contained when the six covered PNG captures were taken — the number a panel would check the slides against.
  - `docs/deck/assets/captures/archive_state.json`

- **Human-gate verification records in no keyFiles list: docs/evidence/PI_VERIFICATION_2026-07-25.md (PI-03/04/05/06/08 combined audit, result 'Local engineering gates passed; external and human gates remain explicit'), docs/evidence/pi-04/VERIFICATION_2026-07-25.md ('VERIFIED' for the normalized catalog against the authorized disposable Supabase project), docs/evidence/security/audit-2026-09-05/README.md (the provenance-and-limits statement for the whole audit bundle), and five SUMMARY.md files under docs/evidence/security/pi-03-*/ and pi-04/live-*/.**
  - The documentation map covers the two CODEBASE_SECURITY_AUDIT_*.md reports but none of the dated receipts they cite. The audit README is the one place that states the reproductions are mocked/offline and that passing means the defect occurred rather than that it is fixed.
  - `docs/evidence/PI_VERIFICATION_2026-07-25.md, docs/evidence/pi-04/VERIFICATION_2026-07-25.md, docs/evidence/security/audit-2026-09-05/README.md, docs/evidence/{security/pi-03-*,pi-04/live-*}/SUMMARY.md`

- **`supabase/.temp/` is listed as a keyFile of the database-schema map but is not a tracked file — `.gitignore:75` ignores `supabase/` outright, and on disk .temp/ holds only Supabase CLI cache values (cli-latest, gotrue-version, linked-project.json, pooler-url, postgres-version, project-ref, rest-version, storage-migration, storage-version).**
  - A phantom citation: no map assertion can be sourced from it, and project-ref/linked-project.json identify the live Supabase project, which is exactly the class of local-only material the repo deliberately keeps out of git.
  - `supabase/.temp/ (untracked; .gitignore:75)`

- **Untracked and in no keyFiles list: paper/Figure_System_Architecture.build.py (301 lines — emits IskAI_System_Architecture.svg at 2000x1350, phase cards with the top row building the archive and the bottom row answering a question, hand-placed for the row-wrap elbow and the wide phase-7 card) and paper/Figure_System_Overview.build.py (208 lines — emits IskAI_System_Overview.svg at 1600x850, deliberately simpler than Figure 8: two flows meeting at one archive and the four places the system may refuse).**
  - These are NEW figure builders, not the tracked Figure1_/Figure8_ pair, and both write to `sys.argv[1] or __file__.parent` — i.e. into paper/ by default, next to the tracked SVGs. Together with the untracked paper/figures/figure_ai_pipeline.png they are an in-progress figure revision that build_corrections.py's REL_TARGETS does not yet reference, so the corrected .docx still carries the old images.
  - `paper/Figure_System_Architecture.build.py, paper/Figure_System_Overview.build.py`

- **Remaining uncovered tracked files, all non-logic, confirmed by inspection rather than by name: 25 PNG/JPG evidence and asset files (docs/deck/assets/captures/*.png, docs/evidence/pi-04/manual-20260725/*.png, docs/evidence/visual-baselines/*.png, docs/evidence/security/audit-2026-09-05/cross-account-cache.png, paper/figures/*.png, frontend public/ and src/assets images), 9 SHA256SUMS.txt / *.stderr.txt / trivy-version.txt receipts, 13 trivy/npm-audit/pip-audit/build-metadata/multiarch-digest JSON and 2 *-build.log files, rag-thesis-frontend/package-lock.json, paper/paper_ORIGINAL_2026-08-09.docx + paper_CORRECTED.docx + paper_CORRECTED.pdf, paper/Figure1_RAG_Architecture.svg, and 3 .gitignore files (docs/deck/.gitignore = `__pycache__/`, paper/.gitignore = `_scratch*` + `__pycache__/`, rag-thesis-frontend/.gitignore).**
  - Listed for closure: these are the files the lens excludes, and they account for the whole remainder, so after the entries above no tracked file in the repo is unaccounted for.
  - `repo-wide (102 of the 173 uncovered tracked files)`


## C2

<a id="c2"></a>

**Lens.** User-facing features and capabilities present in the code but absent or under-described in the subsystem maps and flow traces

**18 findings.**

- **Whole second answering mode in chat: deterministic archive inventory / count / paginated enumeration, with a conversation-level listing cursor. Asking "how many theses are indexed?", "list the theses available here", or the Filipino/Ilocano equivalents bypasses pgvector AND the LLM entirely and is answered from `list_archive_papers` with a hand-built markdown numbered list carrying `[n]` markers; a follow-up ("any others", "what are those?", "is that all") continues from an offset derived by replaying the session history. Page size is 10 (`_ARCHIVE_INVENTORY_LIMIT`). It returns `kind='answer'` with real `sources`, so those sources become the `reference_sources` that "number 2" later indexes into.**
  - The chat map describes the fast paths as "greeting, identity, thanks, farewell, capabilities; these return kind='notice' with no retrieval". This path is none of those: it retrieves (from the papers table), returns an answer not a notice, ships sources, and is stateful across turns. No map or flow mentions it, yet it is the answer a user gets for the most obvious question they can ask the archive.
  - `rag-thesis-backend/routers/chat.py:510 (_ARCHIVE_INVENTORY_LIMIT=10), :874 (_is_archive_inventory_question), :940 (_matches_local_count), :947 (_is_archive_count_confirmation), :957 (_is_archive_count_question), :982 (_is_archive_continuation_question), :1015 (_archive_listing_cursor), :1045-1119 (_archive_inventory_response), :2282-2320 (the dispatch); rag-thesis-backend/services/retriever.py:407 (list_archive_papers)`

- **Every ordinary chat question also runs an 85%-threshold topic-duplication check in parallel with retrieval, and a flagged hit renders a red "Potential topic duplication — NN.NN% similarity" card naming the matched thesis, plus a second Gemini call (`_summarize_duplication`) that writes a prose summary of the matched study. The alert is persisted into `chat_messages.duplication_alert` and re-rendered when the session is reopened. For guests it charges the token budget a second time.**
  - The novelty flow dismisses this as "the third, unrelated check_topic_duplication path used only by chat" and the maps only list the function signature. Nothing describes it as a user-facing feature — that proposing a topic in chat produces a plagiarism-style advisory with its own LLM-written blurb, or that it costs a second model call on the critical path of every retrieval question.
  - `rag-thesis-backend/routers/chat.py:1969-1986 (check_duplication gathered with search_chunks), :1888 (_summarize_duplication), :1998-2001, :2713-2729, :2902; rag-thesis-frontend/src/pages/Chat.jsx:365-400 (DuplicationBanner), :766`

- **Twelve distinct labelled conversational fast paths exist, not five. Four are entirely undescribed: `model_identity` ("what model are you?" → a reply that interpolates `settings.gemini_chat_model`, so any guest can read the configured model name), `archive_inventory`, `system_identity` (Filipino "ano ba itong system na ito", gated on there being no reference_sources), and `self_platform` (a deliberately ungated correction when the user names the platform itself).**
  - `_model_response()` is built inline in chat.py and is NOT a member of `chat_notices.NOTICE_MARKERS`, so `response_kind()` classifies it as `answer` and it is persisted as one — it then re-enters the transcript as answer-kind history. The maps enumerate NOTICE_MARKERS exhaustively but never note this one message that deliberately sits outside it.
  - `rag-thesis-backend/routers/chat.py:2126-2135 (model_identity), :2147, :2166, :2180, :2198, :2307, :2329-2341 (system_identity), :2346-2360 (self_platform), :2412, :2592, :2664; the model reply text at :854-859`

- **Prompt editing / conversation branching. A user can click the edit pencil on any earlier prompt, rewrite it, and resend; the client computes the turn index and the server DELETES that saved turn and every later one before re-answering, permanently destroying the rest of the branch.**
  - `edit_from_turn` appears in the models list and `branchBeforePrompt` in the frontend surface, but no flow traces the edit round-trip, and nothing states that editing a prompt is destructive to the persisted transcript.
  - `rag-thesis-frontend/src/pages/Chat.jsx:449-540 (UserBubble inline editor), :992, :1009 (branchBeforePrompt), :1410; rag-thesis-frontend/src/pages/chat/transcript.js:139; rag-thesis-backend/routers/chat.py:2035 (400 'A prompt edit requires a saved session.'), :2044-2050, :1530-1554 (_truncate_session_from_turn), :1439 (_history_before_turn); rag-thesis-backend/models.py:38`

- **Per-answer action menu in chat: Redo response (regenerate), Copy response, Listen (browser SpeechSynthesis TTS over a de-markdowned transcript), Copy with sources, Download research note (.txt blob named `iskai-research-note.txt`), Ask a follow-up, and a "Response details" modal exposing response type, generation path, citation count, evidence-passage count, distinct archived studies, archive basis and timestamp.**
  - The functions are listed in the chat-experience map, but no flow says a user can hear an answer read aloud, export it as a file, or open a provenance panel. The details modal in particular is a user-visible surface for pipeline internals.
  - `rag-thesis-frontend/src/pages/Chat.jsx:579-725 (AiResponseActions + details Modal), :610-639 (speechSynthesis); rag-thesis-frontend/src/pages/chat/responseActions.js:40 (plainResponseText), :50 (copyText), :66 (responseWithSources), :79 (downloadResearchNote), :91 (responseDetails)`

- **Novelty page has three capabilities beyond the scan itself: (a) a per-scan follow-up Q&A conversation (`POST /duplication/chat`, 20/minute, owner-scoped, appends to `scan_history.chat_log`, newest 20 returned); (b) scan history browse, delete-one and clear-all; (c) plain-text `.txt` manuscript upload as an alternative to PDF; plus a superadmin-only department selector that re-targets the scan.**
  - The novelty flow stops at "the rendered result and the JSON report". The conversational layer over a stored scan is a second LLM surface with its own rate limit and its own persisted log, and none of the history-management actions are described anywhere.
  - `rag-thesis-backend/routers/duplication.py:54 (DuplicationChatReq), :463-550 (duplication_chat), :552 (history), :576 (delete one), :603 (clear all), :230-237 (.txt accepted); rag-thesis-frontend/src/pages/Novelty.jsx:39 (SCAN_MIME_TYPES incl. text/plain), :69, :84 (accept=".pdf,.txt"), :145 (scanDuplicationChat), :333, :348, :365, :409-412 (superadmin dept Select)`

- **Archive detail modal renders a duplication-screening provenance panel that contrasts the at-upload screening against the current post-ingest rescan, with a collapsible "What the screening saw at upload" history block and an explanation of why the two differ. This is the only place a user sees the effect of `novelty.refresh_after_ingest` / `store_rescan`.**
  - The archive flow is described as "browsing, filtering and reading". The helpers are listed in the design-system map as bare names. Nothing says the archive surfaces a per-paper screening verdict with drift history, which is the user-visible output of a worker-side background mutation.
  - `rag-thesis-frontend/src/pages/Archive.jsx:239-300 (ScreeningDetail), :475-508 (ArchiveDetailModal); rag-thesis-frontend/src/lib/utils.js (hasRescan / currentScreening / atUploadScreening / screeningIsUnchanged / screeningHasDrifted / isScreeningFlagged / verdictLabel / verdictExplanation); rag-thesis-backend/services/novelty.py:416 (store_rescan), :446 (refresh_after_ingest)`

- **Seven backend endpoints have no frontend binding at all and are reachable only through the always-public interactive docs (`/docs` is never disabled — no `docs_url=None` anywhere): GET /maintenance/storage-cleanup, POST /maintenance/storage-cleanup/{id}/retry (deletes storage objects and paper rows), POST /maintenance/retention/run?apply=true (destructive purge of upload_job_events / resolved alerts / security_audit_events), POST+PATCH /catalog/programs, POST+PATCH /catalog/specializations (including `active: false`, which silently removes programs from the public landing marquee, archive filters and upload pickers), and GET /catalog/departments (the `contract_version` envelope).**
  - The admin flow narrates the four React tabs; the maps list these routes as surface but no flow explains who invokes them or how. The CORS comment proves /docs is the intended invocation path, which makes Swagger UI an undocumented admin surface.
  - `rag-thesis-backend/routers/maintenance.py:121, :144, :156; rag-thesis-backend/routers/catalog.py:181 (GET /departments), :193 (POST /programs), :208 (PATCH /programs), :220 (POST /specializations), :232 (PATCH /specializations); rag-thesis-backend/models.py:259-261 (CatalogEntityUpdate: name + active); rag-thesis-backend/main.py:142-151 (FastAPI() with no docs_url override); rag-thesis-backend/main.py:187-197 (CORS comment explicitly says PATCH was added because "a documented superadmin operation was unreachable from any browser while working perfectly from curl and the OpenAPI docs page"); rag-thesis-frontend/src/api.js:395-418 (no bindings)`

- **PUT /analytics/me accepts and validates `program_id` / `specialization_id` (through `resolve_academic_selection` with `require_program=True` for students, 422 on a specialization that does not belong to the program, 503 when the catalog migration is missing) but NO frontend surface ever sends them. ProfileSection only ever posts `full_name` and `avatar_url`.**
  - `profiles.program_id`/`specialization_id` are indexed columns with a validating BEFORE-UPDATE trigger and are read back by GET /analytics/me, yet a student can only populate them via /docs or curl. No map or flow notes that this half of the profile contract has no UI.
  - `rag-thesis-backend/routers/analytics.py:550-570; rag-thesis-backend/services/catalog.py (resolve_academic_selection); rag-thesis-frontend/src/pages/settings/ProfileSection.jsx:50, :76, :136 (the only three updateMyProfile call sites); rag-thesis-frontend/src/api.js:356`

- **Settings is a six-section console and four of the six are undescribed as features: Chat (localStorage-only default thesis-category filter and an Enter-vs-Ctrl+Enter send-key toggle, plus a "Clear all conversations" bulk delete that fans out one DELETE /sessions/{id} per session via Promise.allSettled and reports partial failure), Appearance, Privacy (principles copy plus an "Account removal" button that is a `mailto:ccsict@isu.edu.ph` link — there is no self-service deletion), and About.**
  - The maps list the section components as exported names only. No flow covers the settings surface, so the bulk-delete (N sequential authenticated DELETEs from one click) and the fact that account deletion is a mailto stub are both unrecorded.
  - `rag-thesis-frontend/src/pages/Settings.jsx:16 (SECTIONS registry); rag-thesis-frontend/src/pages/settings/ChatSection.jsx:38-88 (ClearConversations), :89-153; rag-thesis-frontend/src/pages/settings/PrivacySection.jsx:44-53; rag-thesis-frontend/src/lib/chatPrefs.js:231, :240`

- **Settings → Security exposes four self-service account operations: change email (Supabase `updateUser({email})` then a 6-digit `email_change` OTP verify), change password (email-code gated), "Sign out on all other devices" (`signOut({scope:'others'})`), and TOTP 2FA enrol/unenrol via a QR dialog. Separately, ProfileSection uploads and deletes avatars directly into the public Supabase `avatars` bucket with client-side MIME/2 MB validation and orphan cleanup on failure.**
  - The account-lifecycle flow runs "sign-up → verification → approval → sign-in + second step → authorization → idle expiry → sign-out". Credential rotation, session revocation and avatar storage are all outside that chain, and avatar upload is the only path in the product where the browser writes to Supabase Storage directly.
  - `rag-thesis-frontend/src/pages/settings/SecuritySection.jsx:30, :47, :115, :131, :185-221, :233; rag-thesis-frontend/src/components/MfaEnrollDialog.jsx; rag-thesis-frontend/src/components/TwoFactorSettings.jsx; rag-thesis-frontend/src/pages/settings/ProfileSection.jsx:19-120 (AvatarEditor)`

- **Cloudflare Turnstile is not only a guest-chat gate. `useSecurityGate` + `SecurityCheck` are wired into six Supabase Auth actions with distinct action labels — `signin`, `signup`, `password_reset`, `password_reset_resend`, `password_reset_verify`, and OTP sign-in — and the token is passed to Supabase as `options.captchaToken`, never to the FastAPI backend.**
  - The auth/abuse map covers only `services/turnstile.py` and `turnstile_expected_action='guest_chat'`, and the chat map notes "useSecurityGate … Not used by chat" without saying what does use it. The backend's expected-action check is irrelevant to these five, because the verification happens at Supabase.
  - `rag-thesis-frontend/src/pages/auth/SignInForm.jsx:34, :208; SignUpForm.jsx:33, :274; ForgotPasswordStep.jsx:31, :139, :192, :235; OtpSignInStep.jsx:26, :79; rag-thesis-frontend/src/pages/auth/authUtils.js (authOptions); rag-thesis-frontend/src/components/security/useSecurityGate.js:372`

- **App-wide UX capabilities in the shell: a Ctrl/Cmd+K command palette (fuzzy search over every permitted nav destination plus "Appearance and energy" and "Profile and security"), an Appearance dialog that mutates a five-axis preference set (theme, palette, motion, effects, contrast) persisted under `isu-thesis-preferences-v2`, a live health indicator polling GET /health every 30 s and rendering Online/Degraded/Offline, and hover/focus route prefetching that warms both the lazy chunk and its React Query data.**
  - The design-system map lists these as component signatures. No flow explains that there is a keyboard-driven navigator, that appearance is a persisted five-axis user preference (the four theme states the a11y suite tests are a *user* setting, not just a test matrix), or that the sidebar continuously polls a backend endpoint.
  - `rag-thesis-frontend/src/components/AppShell.jsx:33-49 (HealthStatus), :139-147 (Ctrl+K), :161-171, :186-198, :313-314; rag-thesis-frontend/src/components/CommandPalette.jsx:7; rag-thesis-frontend/src/components/AppearanceDialog.jsx; rag-thesis-frontend/src/context/PreferencesContext.jsx; rag-thesis-frontend/src/lib/routePrefetch.js:236`

- **Idle-session enforcement is a user-facing interruption: a warning modal with a depleting countdown ring and a "Stay signed in" button appears 60 s before a role-dependent idle deadline, there is a 12-hour absolute session cap independent of activity, and on expiry the user is signed out and redirected to `/login?next=<path>` so the original destination is restored after re-auth.**
  - The account-lifecycle flow lists "idle expiry" as one word. The privileged-vs-standard idle limits, the 60-second grace modal, the absolute cap and the `?next=` round-trip are four separate behaviours a user will hit, none of them described.
  - `rag-thesis-frontend/src/components/IdleSessionGuard.jsx:24, :53-101, :199; rag-thesis-frontend/src/lib/idleSession.js:7 (IDLE_LIMIT_MS per role), :13 (IDLE_WARNING_MS 60 s), :17 (ABSOLUTE_SESSION_MS 12 h), :23 (idleLimitForRole), plus safeNextPath / loginPathWithNext / formatCountdown`

- **Batch upload has two capabilities the batch flow does not cover: an in-flight batch is serialized to `localStorage` under `activeUploadBatch` (owner-scoped, rejected on owner mismatch or the legacy bare-array shape) so a page reload rehydrates the rows and resumes polling; and each row can be cancelled individually via POST /upload/jobs/{id}/cancel. The single-upload wizard has the same cancel button, and also links out to /upload/batch.**
  - The batch flow corrects three premises about concurrency and stage rendering but never mentions crash/reload recovery or per-row cancellation, and the maps list `serializeActiveBatch`/`restoreActiveBatch` as signatures only. The synthetic `expired` status after 3 missing polls is also a user-visible state with no server counterpart.
  - `rag-thesis-frontend/src/pages/upload/batchState.js:37 (ACTIVE_BATCH_STORAGE_KEY), :383 (serializeActiveBatch), :400 (restoreActiveBatch), :34-36 (MISSING_POLLS_BEFORE_EXPIRED=3); rag-thesis-frontend/src/pages/UploadBatch.jsx:990 (cancelUploadJob); rag-thesis-frontend/src/pages/Upload.jsx:408 (cancel), :444-448 (link to /upload/batch)`

- **The admin Overview tab renders a full Objective-2 research-evidence table in-product: five strata × {n, baseline, RAG, change, p-value, significant/not}, a gold "the figure the paper quotes" badge on the topic-present stratum, a "How to read this" caveat paragraph, and faithfulness / context-precision diagnostics — all read from a committed JSON file that is currently UNTRACKED in git.**
  - The evidence flow states "the paper carries none of the Objective 2 measured figures" and treats the export as a build artifact. It does not say the running admin console publishes the per-stratum result with p-values to every admin, nor that the data file backing that panel is not in the repository — so a fresh clone fails to build this page.
  - `rag-thesis-frontend/src/pages/admin/AdminOverview.jsx:185-290; rag-thesis-frontend/src/data/objective2Summary.json (git status: `?? rag-thesis-frontend/src/data/`, AdminOverview.jsx: ` M`); rag-thesis-backend/scripts/export_objective2_summary.py:126-152`

- **Three health/readiness surfaces are unauthenticated and invocable by anyone, and two of the three are consumed by nothing in the repo: GET /health/worker (one uncached read of `ingestion_workers`, returns 503 when no worker has heartbeat within `operations_worker_stale_seconds` or the scanner is unavailable), GET /ready (deliberately bypasses the 15 s contract cache, so each call costs four DB reads), and GET /healthz on the frontend nginx container.**
  - The maps list all three as surface but nothing identifies them as an externally reachable, uncached, unauthenticated operational oracle — /ready in particular is a 4-read-per-request endpoint behind only the global 120/minute limit, and /health/worker discloses ingestion-fleet liveness to anonymous callers.
  - `rag-thesis-backend/main.py:289 (/health/worker), :311 (/ready); rag-thesis-frontend/nginx/default.conf.template:20 (location = /healthz); rag-thesis-frontend/src/api.js:161 (only /health is bound); rag-thesis-backend/Dockerfile HEALTHCHECK (only /health)`

- **Two ingestion gates produce user-visible upload rejections that no flow explains: `max_pdf_pages` (default 500) rejects a PDF with HTTP 422 "PDF exceeds the 500-page safety limit" at three separate entry points, and `require_ocr_for_scanned_pages` (default True) fails an already-queued ingestion job when any page needed OCR and OCR could not run — so on a host without the tesserocr wheel a valid manuscript fails asynchronously, after the user has already seen a 202 and is polling.**
  - The upload and ingestion flows list the eight pipeline stages but not these two refusal conditions. The OCR one is the only case where a job that passed every synchronous validation still fails for an environment reason, and the resulting failure message is what the polling UI shows the uploader.
  - `rag-thesis-backend/routers/upload.py:510-511; rag-thesis-backend/routers/duplication.py:191-193; rag-thesis-backend/services/ingestion.py:81, :127; rag-thesis-backend/config.py:115 (max_pdf_pages), :124 (require_ocr_for_scanned_pages, with the 2026-09-07 measurement comment)`


## C3

<a id="c3"></a>

**Lens.** Contracts, invariants and coupling points: frozen constants and version strings, exact literals matched across the frontend/backend boundary, DB RPC signatures the Python calls by name, stage-name strings shared between worker/SQL/UI, env vars that change behaviour, proxy/CSP entries that must track router prefixes, and test assertions that freeze a literal.

**17 findings.**

- **CORS allow_headers / expose_headers / allow_methods are hand-maintained lists that must track every custom header the frontend sends and every method a router declares — and NO test pins any of the three. Change api.js to send a new header (or add a router verb) and cross-origin browser use breaks while curl, the OpenAPI docs page and all 2143 backend tests pass.**
  - The comment at main.py:191-196 records that this exact failure already shipped once: PATCH was missing from allow_methods, so PATCH /catalog/programs and /catalog/specializations were unreachable from any browser while 'working perfectly from curl and the OpenAPI docs page'. The fix added a comment, not a test — the next omission fails the same silent way. expose_headers is the worse half: drop X-Guest-Verification and the guest Turnstile gate at useGuestChatGate.js:67 goes permanently deaf (the header is invisible to JS), so guests see a bare 403 with no challenge and no retry path.
  - `rag-thesis-backend/main.py:197 (allow_methods), :198 (allow_headers = Authorization, Content-Type, X-Guest-ID, Idempotency-Key, X-Turnstile-Token), :200 (expose_headers = X-Guest-Verification) ↔ rag-thesis-frontend/src/api.js:79,87 (X-Guest-ID), :191 (X-Turnstile-Token), :256 (Idempotency-Key), src/pages/chat/useGuestChatGate.js:67 (reads response header x-guest-verification). Producer side: services/turnstile.py:28 VERIFICATION_HEADER, :119, :122; routers/upload.py:898 Header(alias='Idempotency-Key'). `grep -rn "allow_headers|expose_headers|allow_methods" tests/*.py` returns nothing.`

- **The upload-job `stage` vocabulary is produced by three independent sources (Python pipeline, SQL RPC bodies, and the table default), consumed by two divergent frontend maps, has NO CHECK constraint on the column, and is pinned by exactly one test — on the frontend side only. Rename a stage in ingestion.py and every backend test still passes while the timeline renders inert.**
  - Three-way split with a one-sided guard. wizardSteps.js:27-32 carries a comment describing the last break ('findIndex returned -1 for it, so the whole stepper rendered inert and greyed while the progress bar already showed movement') — caused by `store`, which is emitted only from SQL and so is invisible to anyone reading the Python pipeline. batchState.js has no equivalent alias table and no test at all, so the two frontend maps can drift from each other as well as from the backend. Nothing on the backend enumerates the legal stage set, so `pytest` cannot catch a rename or a new stage.
  - `Producers: rag-thesis-backend/services/ingestion.py:103,111,121,141,157,169,238 emit download/malware_scan/extract/chunk/embed/screen/index; SQL emits store (supabase_setup.sql:1475, migrations/20260723_durable_ingestion_jobs.sql:130), queued (supabase_setup.sql:1509), download (:1575), done (:1704), error (:1535,:1546,:1652), cancelled (migrations/20260724_operations_security.sql:194,261,369); column default is a *fourth* value, 'extract' (supabase_setup.sql:780, migrations/20260719_production_hardening.sql:93). Consumers: rag-thesis-frontend/src/pages/upload/wizardSteps.js:18-25 (PIPELINE_STAGES, 7 keys) + :33 (STAGE_ALIASES for store/queued/''), src/pages/upload/batchState.js:361-364 (STAGE_LABELS, 12 keys). Only pin: src/pages/upload/wizardSteps.test.js:12-14.`

- **The frozen preprocessing/chunking version strings and token sizes are hardcoded into SQL in six places across two files (a CHECK constraint, a legacy-backfill CASE, and the commit-time validation) with no test comparing them to the Python constants they mirror. Bump CHUNKING_VERSION in Python and every single ingestion commit starts raising at runtime, with a green test suite.**
  - CLAUDE.md lists CHUNKING_VERSION and PREPROCESSING_VERSION as bump-and-record constants without naming the six SQL sites that must move with them. The failure mode is the worst shape available: commit_paper_ingestion raises 'Verified 768-dimensional index provenance is required' for every upload, the job retries three times through _RETRY_DELAYS and lands in `failed`, and nothing in CI reproduces it because the SQL is never executed by the unit suite. The one test that reads both languages (test_schema_consistency.py) is scoped to SQL-vs-SQL drift only.
  - `Python: rag-thesis-backend/services/chunker.py:21 TOKENIZER_ENCODING='cl100k_base', :22 CHUNKING_VERSION='token-v1'; services/index_provenance.py PREPROCESSING_VERSION='document-v1'; config.py:74-75 Literal[800]/Literal[100]. SQL: supabase_setup.sql:335-337 (CHECK verified_index_provenance_is_current), :357-363 (backfill CASE), :489-491 (commit_paper_ingestion validation) and the same three blocks at migrations/20260720_index_embedding_provenance.sql:24-26, :52-58, :133-135. tests/test_index_provenance.py:14-24 pins only the Python dict; tests/test_schema_consistency.py compares base-vs-migration SQL but never SQL-vs-Python.`

- **The role×feature authorization matrix exists in four independent hardcoded copies, and no test compares any pair. The copy that actually builds the PUT payload is two inline array literals in a JSX file, and the backend validator requires the payload key set to be *exactly* right or it 422s.**
  - Adding a fifth feature (or a third role) to the backend means copy (4) renders an incomplete grid and every superadmin save 422s, because _validated_features demands the exact {student,faculty} × exactly-four-boolean shape. Copy (2) is the production seed, so a mismatch there silently grants or withholds a feature in a deployed database while DEFAULT_FEATURES says otherwise and the fallback path never fires. Four copies, zero cross-checks, and the most fragile copy is the least visible.
  - `(1) rag-thesis-backend/routers/settings.py:18-21 DEFAULT_FEATURES + :22-23 _FEATURE_ROLES/_FEATURE_NAMES; (2) rag-thesis-backend/supabase_setup.sql:746 seeded role_features jsonb; (3) rag-thesis-frontend/src/lib/permissions.js:10-13 DEFAULT_ROLE_FEATURES; (4) rag-thesis-frontend/src/pages/admin/SystemManagementTab.jsx:90 `['student','faculty']` and :95 `['chat','archive','novelty','upload']`. Validator: routers/settings.py::_validated_features. tests/test_hardening_fixes.py:184 asserts only that the read endpoint falls back to copy (1); permissions.test.js:46-47 asserts only that copy (3) is frozen.`

- **The `match_chunks` RPC parameter dict is retyped by hand at three call sites with no shared builder, and every test asserts the parameter names against a fake client rather than against the SQL signature. Two of the three sites also silently omit p_thesis_category, and one of those has no parameter assertion at all.**
  - supabase-py passes RPC arguments by name, so renaming p_department in SQL is a runtime-only failure that all 2143 tests miss (the stubs happily accept any key). Three independent dicts mean adding a required parameter is a three-file edit with no mechanical reminder. Separately, because duplication.py and novelty.py omit p_thesis_category entirely, both the interactive scan and the ingest-time screen compare a submission against student *and* faculty theses regardless of its own category — a semantic divergence from the retrieval path that is invisible at the call sites because the parameter is simply absent rather than passed as null.
  - `rag-thesis-backend/services/retriever.py:786-795 (only site that conditionally adds p_thesis_category, :796-797) ↔ routers/duplication.py:135-140 ↔ services/novelty.py:353-358. SQL signature: migrations/20260903_hnsw_ef_search.sql:35. Tests: tests/test_retriever.py:142-143 and :371-372 assert p_department / p_embedding_model on a stub; `grep -n 'p_embedding_model|match_chunks' tests/test_novelty.py` finds only a prose mention at :187 — novelty.py's call site is unasserted.`

- **The operations alert engine runs concurrently in two processes under the documented production topology — the exact cross-process race that main.py's own docstring claims to have fixed for the single-process case.**
  - main.py's docstring states the hazard verbatim — 'Both would race on `upsert_alert` / `resolve_alert` and an alert could flap between open and resolved' — and then guards only against a second thread inside the same interpreter. The compose file turns the flag on for both containers, so in production two processes call evaluate_operations on independent schedules. The upsert side is protected by ON CONFLICT, but resolve_alert is an unguarded UPDATE and notify_webhook has no idempotency key, so the observable symptoms are duplicate webhook deliveries and open/resolved flapping. Nothing couples the compose env block to the single-owner assumption the code documents.
  - `rag-thesis-backend/main.py:84 gates the monitor thread on settings.operations_monitor_enabled; rag-thesis-backend/workers/ingestion_worker.py:239 calls evaluate_operations(self.client) under the same flag; docker-compose.operations.yml:26 sets OPERATIONS_MONITOR_ENABLED: "true" on the `api` service and :43 sets it on the `worker` service. Defence in main.py:78-93 is per-process only (_OPERATIONS_STATE thread handle). Atomic upsert: migrations/20260724_operations_security_fixes.sql:7 upsert_operational_alert; non-atomic: services/operations.resolve_alert (plain UPDATE) and notify_webhook (no dedupe).`

- **ChatRequest's list-length and per-item caps are duplicated as bare slice literals in the frontend, at three call sites, with no shared constant and no test tying them together. Exceed any of them and the whole turn fails with a 422 the chat UI surfaces as a generic field error.**
  - Four numbers (5, 10, 30, 500) live on both sides of the wire with nothing linking them. Lowering a backend cap turns every guest follow-up into a 422 until someone notices the matching slice; raising one is silently ineffective. The 500 is the sharpest case: models.py:44 and Chat.jsx:66 are the same magic number written twice, and the trimming filter exists *only* on the client, so any other client (or a replay of a stored transcript) can exceed it.
  - `Backend: rag-thesis-backend/models.py:28-30 guest_history max_length=5 (item max 4000), :32-34 guest_source_ids max_length=10 (item max 64), :42-45 conversation_replies max_length=30 (item max 500). Frontend: src/pages/Chat.jsx:1123 `.slice(-5)`, :1135 `.slice(0, 10)`, :1149-1150 `.filter(withinReplyLimit).slice(-30)`, and a second independent copy at :1020 `.filter(withinReplyLimit).slice(-30)`; the 500 is re-declared at Chat.jsx:66 `CONVERSATION_REPLY_MAX_CHARS = 500`.`

- **The LangSmith tracing/redaction bootstrap exists only in the API process. The worker shares the same env_file, so it inherits LANGSMITH_TRACING but never derives langsmith_hide_inputs/hide_outputs from settings — and those defaults are True in config while commented out in .env.example.**
  - Turning on tracing is a one-line .env change that has different privacy semantics in each process. In the API, HIDE_INPUTS/HIDE_OUTPUTS are materialised from the safe `True` defaults. In the worker, LangChain reads LANGSMITH_TRACING straight from the inherited environment while the hide flags are simply absent — so the process that handles full manuscript text (extract → chunk → embed) is the one that traces it unredacted. The coupling is invisible because the protective default lives in config.py and only main.py ever pushes it into the environment. Also note main.py uses setdefault, so a pre-set LANGSMITH_HIDE_INPUTS=false in the environment wins over the code default in both processes.
  - `rag-thesis-backend/main.py:42-52 (os.environ.setdefault for LANGSMITH_TRACING / API_KEY / PROJECT / HIDE_INPUTS / HIDE_OUTPUTS + the LANGCHAIN_* aliases); rag-thesis-backend/workers/ingestion_worker.py has no equivalent block (imports os at :8, never writes these). config.py:195-196 langsmith_hide_inputs/langsmith_hide_outputs default True; rag-thesis-backend/.env.example:188-189 ships both commented out. docker-compose.operations.yml:22 and :37 give api and worker the identical `env_file: ./rag-thesis-backend/.env`.`

- **The two deployed surfaces set different Referrer-Policy values, and the header validator's regex accepts only the nginx one — so running the documented check against the API host reports a failure that is actually a deliberate (stricter) choice, with nothing recording which value is canonical.**
  - `npm run security:headers -- <api-origin>` fails on a correct deployment. The script is never run by CI (.github/workflows/quality.yml has no security:headers step), so the mismatch is only discovered by whoever follows the runbook, and the regex silently encodes the frontend value as the only acceptable one. tests/test_operations_security.py:275-283 checks that the nginx template *declares* a Referrer-Policy header but not what value, so neither side is pinned.
  - `rag-thesis-backend/main.py:167 sets `Referrer-Policy: no-referrer`; rag-thesis-frontend/nginx/default.conf.template:17 sets `Referrer-Policy "strict-origin-when-cross-origin"`; rag-thesis-frontend/scripts/validate-security-headers.mjs:14 pins `/strict-origin-when-cross-origin/i`. Same split on Permissions-Policy: main.py:168 lists three directives, the nginx template lists five (adds payment, usb) — the validator's regex `/camera=\(\).*microphone=\(\)/i` happens to match both.`

- **Two of the three envsubst placeholders the production CSP depends on are undocumented in every .env.example and the README, the multiarch verifier requires a different four-variable set, and the nginx test pins only two of the three placeholders. An unset variable yields a syntactically valid CSP that blocks the API.**
  - envsubst replaces a missing variable with the empty string, so an unset SUPABASE_WS_ORIGIN produces `connect-src 'self' https://api... https://x.supabase.co https://challenges.cloudflare.com` — valid, quiet, and missing the websocket origin that AuthContext's `channel('global_feature_updates')` realtime subscription needs. Three placeholders, two pinned by a test, two absent from the documented env surface, and the one verifier script that checks required build variables omits the third. The nginx comment block at :7-12 carefully documents the *font* origins and says nothing about which variables must be set.
  - `rag-thesis-frontend/nginx/default.conf.template:14 references ${API_ORIGIN}, ${SUPABASE_ORIGIN}, ${SUPABASE_WS_ORIGIN} (and ${DOLLAR} at :27,:33); docker-compose.operations.yml:58-61 supplies them from PUBLIC_API_ORIGIN / VITE_SUPABASE_URL / SUPABASE_WS_ORIGIN / $$. Neither PUBLIC_API_ORIGIN nor SUPABASE_WS_ORIGIN appears in rag-thesis-frontend/.env.example (the only .env.example on the frontend side; there is no root .env.example) or anywhere in README.md. scripts/verify-pi03-multiarch.ps1:123 requires PUBLIC_API_ORIGIN, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_TURNSTILE_SITE_KEY — not SUPABASE_WS_ORIGIN. tests/test_operations_security.py:282 asserts only '${API_ORIGIN}' and '${SUPABASE_ORIGIN}'.`

- **config.py treats the six 'frozen' RAG constants asymmetrically: three are Literal types that crash the process on env drift, three are plain Fields with wide validation bounds that accept a silent production override. CLAUDE.md lists all six together as frozen.**
  - RETRIEVAL_THRESHOLD=0.5 or DUPLICATION_THRESHOLD=0.5 in a .env is accepted at startup, leaves no trace in the index provenance, and changes the measured pipeline the paper reports. The Literal three fail loudly; the Field three fail silently. The only guard is evaluation/run_comparison.py:422-446 (`answering_configuration` includes rag_contract, and guard_checkpoint_provenance refuses a resumed checkpoint whose configuration moved) — which fires only when someone resumes an evaluation run, not at API startup, not in CI, and not in the release fingerprint comparison. Separately, chunk_size_tokens / chunk_overlap_tokens / embedding_dimensions / gemini_capacity_cooldown_seconds / rate_limit_upload / rate_limit_public / operations_alert_timeout_seconds are all env-settable and documented in neither .env.example nor README.md (verified by diffing config.Settings.model_fields against both).
  - `rag-thesis-backend/config.py:21 embedding_dimensions: Literal[768], :74 chunk_size_tokens: Literal[800], :75 chunk_overlap_tokens: Literal[100] — versus :76 retrieval_threshold: Field(default=0.30, ge=0.0, le=1.0), :77 retrieval_match_count: Field(default=5, ge=1, le=20), :78 duplication_threshold: Field(default=0.85, ge=0.0, le=1.0). All six are ordinary pydantic-settings fields, so all six read from the environment. Detection: services/index_provenance.py:11 current_index_fingerprint() stamps chunk_size_tokens/chunk_overlap_tokens into every paper_index_versions row (and the SQL CHECK rejects deviation), but stamps neither threshold nor match_count.`

- **The frontend recognises the privileged-MFA refusal by a lowercase substring of the backend's detail string. The backend string is pinned by a test; the frontend marker is pinned by nothing, and no test relates the two.**
  - privilegedMfa.js:11-13 documents the intent ('Matched as a lowercase prefix so the sentence can gain a clause without going deaf') and that is genuinely robust to appended clauses — but not to rewording the opening, which is exactly the edit a reviewer would make. Because the backend test pins the full sentence and lives in the same repo, a rewording updates the backend test in the same commit, turns green, and leaves PrivilegedMfaGate permanently silent: an admin verified by emailed OTP sees a UI whose every privileged call 403s with no enrolment prompt. CLAUDE.md warns 'do not reword it' and names src/lib/privilegedMfa.js — the instruction is the only enforcement.
  - `rag-thesis-backend/dependencies/auth.py:224 PRIVILEGED_MFA_REQUIRED_DETAIL = 'Multi-factor authentication is required for privileged access.' — pinned by tests/test_auth_authorization.py:200-202 and required of all five guards at :210-223. rag-thesis-frontend/src/lib/privilegedMfa.js:16 PRIVILEGED_MFA_MARKER = 'multi-factor authentication is required', matched at :22 via `detail.toLowerCase().includes(...)`.`

- **The exact interpreter patch version is duplicated across five files with no mechanical cross-check, the Dockerfile comment that names the coupling omits two of them, and the evidence file's own anchor for the CI pin is stale.**
  - The only enforcement is the `RUN python -c "...assert sys.version_info[:3] == (3, 14, 7)"` at Dockerfile:27, which fires in the `containers` CI job — so the Dockerfile and the base-image digest are coupled and checked, but quality.yml:47, README.md:74, build_corrections.py:184 and iso25010_evidence.md:952 are not. tests/test_readme_accuracy.py has 32 assertions about README facts and none about the Python version; tests/test_dockerfile_contents.py checks only the COPY list. The Dockerfile comment records that this coupling already caught one drift (3.14.6 → 3.14.7 on 2026-08-25) via the assertion — the four prose copies were updated by hand.
  - `rag-thesis-backend/Dockerfile:27 (the runtime assertion), .github/workflows/quality.yml:47, README.md:74, paper/build_corrections.py:184 (the paper's version-table row), rag-thesis-backend/evaluation/iso25010_evidence.md:952. The Dockerfile comment at :25-26 says 'Keep .github/workflows/quality.yml, README.md, and the paper's Tables 1-4 in step' — omitting iso25010_evidence.md. iso25010_evidence.md:952 cites `.github/workflows/quality.yml:33`, but :33 is `runs-on: ubuntu-latest`; the pin is at :47 (its `Dockerfile:27` anchor is correct).`

- **The server's `kind` field collides with the frontend's own transcript-role field of the same name, and must be aliased to `messageKind` at every ingest point. One of the two ingest points is frozen by a source-text regex; the other is not.**
  - messageNotice.js:8-12 documents the failure this alias exists to prevent ('reloading a conversation made a capacity apology or a refusal visually identical to a grounded answer') — and that bug was in the loadSession path, which is the one still unpinned. The live path got a source-regex guard; Chat.jsx:1064 did not. A refactor of loadSession that drops or renames the alias restores the original defect, and the only test that would notice is checking the other branch.
  - `Server field: rag-thesis-backend/models.py:58-76 ChatResponse.kind Literal['answer','notice'], plus routers/sessions.py:82-100 returning `kind` per stored row. Frontend's own meaning: src/pages/Chat.jsx:921,1124,1403 use `kind === 'user'` and :1017,1131,1147 use `kind === 'ai'`. Required aliases: Chat.jsx:1162 (live append, `messageKind: res.kind`) and Chat.jsx:1064 (loadSession, `messageKind: m.kind`). Consumer: src/pages/chat/messageNotice.js:33 and src/pages/chat/responseActions.js:63 read `messageKind`. Pin: src/pages/chat/messageNotice.test.js:62 greps Chat.jsx for the literal regex /\.\.\.res, kind: 'ai', messageKind: res\.kind/ — the live path only.`

- **Frozen RAG constants are baked into user-facing UI label prose in two separate, differently-worded frontend files, so the numbers the paper freezes exist as English strings the backend cannot see.**
  - CLAUDE.md's frozen-contracts list names config.py, prompts.py, chunker.py, index_provenance.py and the paper tables, but not these two UI files. Changing any of the three constants leaves the upload wizard and the batch page telling users a number the pipeline no longer uses, in two places with different wording, and neither wizardSteps.test.js nor any backend test compares the label text to config. This is the one copy of the frozen constants that an end user reads directly.
  - `rag-thesis-frontend/src/pages/upload/wizardSteps.js:21 'Chunk (800 tokens)', :22 'Embed (768d)', :23 'Screen novelty (85%)' — and an independent reworded copy at src/pages/upload/batchState.js:362 'Chunking (800 tokens)', 'Embedding (768d)', :363 'Screening novelty (85%)'. Sources of truth: rag-thesis-backend/config.py:74-75 (800/100), :21 (768), :78 (0.85).`

- **Eight backend prefixes are proxied by plain string match in the Vite dev server, so any future React route whose path begins with one of them is swallowed by the proxy instead of routed by the SPA. Only the three known collisions are HTML-aware, and nothing enumerates the constraint.**
  - The three spa-aware entries are retrofits for collisions that already happened; the other eight are a latent trap. Renaming /archive to /papers, or adding an /analytics or /departments page, produces a route that returns JSON in dev and works in the built container (where nginx try_files serves index.html) — a dev-only breakage with no test and no comment naming the rule. The inverse is the documented case in CLAUDE.md ('A new backend router prefix needs a proxy entry here'), which is the same coupling read in one direction only.
  - `rag-thesis-frontend/vite.config.js:93-105: `/upload`, `/chat`, `/settings` use spaAwareApiProxy (HTML bypass); `/papers`, `/health`, `/sessions`, `/duplication`, `/analytics`, `/catalog`, `/maintenance`, `/departments` are plain BACKEND targets. Current React routes (src/App.jsx:44-56): /, /login, /dashboard, /archive, /chat, /novelty, /upload, /upload/batch, /admin, /settings, * — no collision today.`

- **The guest daily token budget is a single global counter keyed on the UTC calendar date, in a store shared with the rate limiter, while the deployment and its UI are Philippine time.**
  - The 'daily allowance' resets at 08:00 local, mid-morning, not at local midnight — so the published guest quota behaves differently from how any operator or defence panel would read it, and nothing documents the timezone choice. Two further couplings ride on the same line: the counter is global rather than per-guest (so one script exhausts every guest's access for the day), and because it shares rate_limit_storage_uri, changing the Redis DB index in that URI silently resets the budget and splits it from the rate-limit keyspace. Without Redis both fall back to memory:// per process, making the 'shared' allowance per-API-worker.
  - `rag-thesis-backend/services/guest_budget.py:49 KEY_PREFIX='guest-token-budget', and the day key is `f'{KEY_PREFIX}:{now.strftime("%Y-%m-%d")}'` built from datetime.now(timezone.utc); the backing store is storage_from_string(settings.rate_limit_storage_uri) — the same URI slowapi uses at services/rate_limiting.py:41. Frontend formats every timestamp en-PH (src/lib/utils.js formatDate). Production requirement: config.validate_production_services demands Redis and a non-zero guest budget.`


## C4

<a id="c4"></a>

**Lens.** Genuine correctness, consistency and security risks not surfaced by the subsystem maps or flow traces: order-dependent logic, cache invalidation, worker/lease races, auth-boundary gaps, duplicated-but-divergent definitions, and silently swallowed errors. Every item below was read in source and cross-checked against its counterpart before reporting.

**13 findings.**

- **The production compose runs the operational-alert state machine in TWO processes concurrently, reintroducing exactly the alert-flap race that `start_operations_monitor`'s docstring claims to have fixed. `docker-compose.operations.yml:26` sets `OPERATIONS_MONITOR_ENABLED: "true"` on the `api` service and `docker-compose.operations.yml:43` sets it again on the `worker` service. The API starts a 60 s thread (main.py:74-93, config.py:142 operations_monitor_seconds=60); the worker calls `evaluate_operations` from `_Housekeeping.tick` on a 300 s cadence (workers/ingestion_worker.py:238-241, config.py:129 ingestion_maintenance_seconds=300); and `GET /maintenance/operations/summary` (routers/maintenance.py:46-50) invokes it a third time on demand. All three write the same `operational_alerts` rows via `upsert_alert`/`resolve_alert` (services/operations.py:88, :136). The fix in main.py:74-93 was scoped only to duplicate threads inside one process - its own docstring says "Both would race on upsert_alert / resolve_alert and an alert could flap between open and resolved" - and the shipped deployment has two processes doing precisely that. Two evaluators can also both read `last_notified_at IS NULL` in the same window (services/operations.py:207-215 `_notify_due`) and each fire the webhook.**
  - A side-effecting GET plus two background evaluators on different cadences makes alert status, occurrence_count and webhook delivery nondeterministic. A superadmin acknowledging an alert can have it reopened by a competing evaluator, and `notify_webhook` does blocking `time.sleep` retries inside the HTTP request thread.
  - `docker-compose.operations.yml:26, docker-compose.operations.yml:43, rag-thesis-backend/main.py:74-93, rag-thesis-backend/workers/ingestion_worker.py:238-241, rag-thesis-backend/routers/maintenance.py:46-50, rag-thesis-backend/services/operations.py:207-215`

- **Two divergent definitions of "worker is healthy", and the looser one is what external uptime monitoring reads. main.py:299-308 (`GET /health/worker`) selects `ingestion_workers` with `.gte('last_seen_at', cutoff).neq('state','stopping').limit(1)` - with NO `ORDER BY` - and calls it healthy when `rows[0].get('scanner_status') != 'unavailable'`. services/operations.py:284-293 requires `scanner_status in {'healthy'}`, adding `'disabled'` only when `malware_scan_mode == 'disabled'`. Consequences: (a) in production (`MALWARE_SCAN_MODE: clamav`) a worker reporting `scanner_status='unknown'` or `'disabled'` is 200-healthy on /health/worker while `evaluate_operations` raises a critical `scanner_unavailable` alert for the same row; (b) with more than one live worker the unordered `.limit(1)` returns an arbitrary row, so a fleet where two of three workers have `scanner_status='unavailable'` can report healthy purely on row order.**
  - The endpoint is documented as "generic worker availability for portable uptime monitoring" - it is the signal an external monitor pages on, and it disagrees with the internal alert engine in exactly the direction that suppresses pages.
  - `rag-thesis-backend/main.py:299-308, rag-thesis-backend/services/operations.py:284-293, rag-thesis-backend/services/operations.py:319`

- **The follow-up retrieval path serves chunk text with no index-provenance verification at all. `retrieval_provenance_params()` is used at only two call sites in the entire backend - services/retriever.py:791 (`search_chunks` -> `match_chunks`) and services/retriever.py:944 (`check_topic_duplication`) - confirmed by grep. `get_paper_overview_context` (services/retriever.py:549), which is the path every resolved follow-up takes ("number 2", ordinal, title fragment, author, quoted title - it bypasses pgvector entirely and reads `chunks` directly), filters only on `paper_id` and `paper.active_index_version` (services/retriever.py:592-597). It never joins `paper_index_versions`, so it never checks `embedding_model`, `embedding_dimensions`, or `provenance_status IN ('verified','legacy_assumed')` - all three of which the `match_chunks` RPC enforces (migrations/20260903_hnsw_ef_search.sql). Worse, the legacy-column fallback at services/retriever.py:610-615 drops the `index_version` filter entirely, so on a 42703 it can return chunks from every index version of the paper interleaved, with duplicate `chunk_index` values feeding citation numbering.**
  - CLAUDE.md states the retrieval contract as "call match_chunks at the fixed 0.30 threshold with provenance params so vectors from another embedding model never match". A paper whose active index is stale or unverified is invisible to semantic search but fully readable through any follow-up reference. services/novelty.py `_candidate_chunk_vectors` deliberately mirrors the provenance predicate, so this is the one retrieval path that does not.
  - `rag-thesis-backend/services/retriever.py:549, rag-thesis-backend/services/retriever.py:592-597, rag-thesis-backend/services/retriever.py:610-615`

- **Guest chat queries are written to `activity_log` with `department = NULL`, so they are excluded from the very counter shown to guests. services/activity.py:19-29 resolves the department from (in order) the `department` argument, `detail['department']`, then a `profiles` lookup keyed on `user_id`. For a guest `user_id` is None, so the profile fallback is skipped. Of the ~14 `chat_query` log sites in routers/chat.py, only two pass `'department'` in the detail dict - line 2308 (archive inventory) and line 2895 (main grounded answer). Every other site omits it: the conversational fast paths at 2127/2143/2162/2176/2194, the listing paths at 2330/2347/2370, and the author path at 2408. `GET /analytics/summary` counts `activity_log` with `{'action':'chat_query','department': settings.thesis_evaluation_department}` (routers/analytics.py:164-167, `_exact_count` at :112-117).**
  - The landing page's "questions answered" figure systematically undercounts by the entire guest fast-path volume. `GET /analytics/activity` and `GET /analytics/logs/system` both filter `.eq('department', profile.department)` for non-superadmins, so a department admin's activity feed silently omits all guest traffic. The non-guest path also pays an extra `profiles` read per logged event because the detail dict does not carry the department the handler already resolved.
  - `rag-thesis-backend/services/activity.py:19-29, rag-thesis-backend/routers/chat.py:2127, rag-thesis-backend/routers/chat.py:2408, rag-thesis-backend/routers/analytics.py:164-167`

- **Per-user rate limiting silently degrades to per-IP whenever `SUPABASE_JWT_SECRET` is unset - a configuration the auth layer explicitly supports. services/rate_limiting.py:12-33 returns `user:<sub>` only when `settings.supabase_jwt_secret` is truthy AND the token is HS256-verifiable; otherwise it falls through to `guest:` (which needs an `X-Guest-ID`, and src/api.js:79-85 sends that only for unauthenticated sessions) and finally `ip:<addr>`. config.py:186 defaults `supabase_jwt_secret` to `''`, and `validate_production_services` (config.py:243-263) does not require it - while dependencies/auth.py:186-208 exists specifically to support "Supabase projects using newer asymmetric signing keys" where "the secret cannot be held locally".**
  - On an asymmetric-key project every signed-in user behind one campus NAT shares a single `ip:` bucket: one 120/minute global default and one 30/minute rate_limit_chat bucket for the entire faculty. Production start-up refuses without Redis, MFA, ClamAV and a guest budget, but says nothing about the secret that makes per-user limiting work at all.
  - `rag-thesis-backend/services/rate_limiting.py:12-33, rag-thesis-backend/config.py:186, rag-thesis-backend/config.py:243-263, rag-thesis-backend/dependencies/auth.py:186-208`

- **`refresh_after_ingest` performs an unsynchronised read-modify-write on `papers.duplication_scan` for every other paper in the department. services/novelty.py:416-431 `store_rescan` reads the existing JSON (or takes the caller's stale snapshot), merges `{'rescan': scan}`, and writes the whole column back with a plain `.update(...).eq('id', paper_id)` - no version check, no RPC, no lock. `refresh_after_ingest` (services/novelty.py:446) snapshots the full `others` list up front and then iterates, passing that snapshot as `record` (services/novelty.py:501, :512). Two ingestions in the same department that overlap each enumerate the same `others` and each write; the second write clobbers the first's `rescan` table, so one of the two newly indexed theses silently never appears in anyone else's screening.**
  - Reachable whenever INGESTION_CONCURRENCY > 1 (config.py:136 allows up to 8) or the `worker` compose service is scaled past one replica. The function swallows its exceptions by design (services/novelty.py:485-487 and :514-515), so a lost update produces no error anywhere - it just leaves screening cards permanently understating overlap, which is the exact defect the function's own docstring was written to fix.
  - `rag-thesis-backend/services/novelty.py:416-431, rag-thesis-backend/services/novelty.py:446, rag-thesis-backend/services/novelty.py:501, rag-thesis-backend/services/ingestion.py (tail of process_ingestion_job)`

- **The query-time novelty scan can never report `exact_duplicate`, and the database would reject it if it tried. routers/duplication.py:304 calls `verdict_for_concentration(top_paper_percentage)` without the second argument, so the `exact_duplicate` branch at services/novelty.py:92-100 is unreachable from `POST /duplication/scan`. Meanwhile the ingest-time screen hard-rejects that same case with `DuplicateManuscriptIngestionError` in services/ingestion.py, and the frontend advertises the verdict: `verdictLabel` returns 'Exact duplicate-not indexed' and `verdictTone` maps it to 'critical' (rag-thesis-frontend/src/lib/utils.js:251, :276). Separately, `scan_history.verdict_level` carries `check (verdict_level in ('clear','review_suggested','high_overlap'))` (supabase_setup.sql:705, re-asserted at :723 and migrations/20260717_rag_items_9_16.sql:87), so passing the flag at line 304 would make the insert fail with SQLSTATE 23514 and 500 the whole scan.**
  - A reviewer who pre-scans a verbatim copy of an archived thesis is told "High overlap - faculty review required" rather than "Exact duplicate", so the two screening paths grade the same manuscript differently. The missing CHECK value is a latent trap: the obvious one-line fix at :304 breaks the endpoint instead of fixing it.
  - `rag-thesis-backend/routers/duplication.py:304, rag-thesis-backend/services/novelty.py:92-100, rag-thesis-backend/supabase_setup.sql:705, rag-thesis-frontend/src/lib/utils.js:251`

- **Role-feature and role caches are per-process with no cross-process invalidation, in a deployment whose production validator presumes shared state. dependencies/auth.py:324-348 holds `_FEATURES_CACHE` with a 60 s TTL and NO lock (unlike `_ROLE_CACHE`, which is lock-guarded at :33). `invalidate_features_cache()` is called only from routers/settings.py:75, i.e. inside whichever process served the `PUT /settings/features`; likewise `invalidate_role_cache` from routers/analytics.py:353/405/461. Nothing on the backend subscribes to the Supabase realtime channel the frontend uses (`global_feature_updates`). config.py:246-247 refuses to start in production without a shared Redis rate-limit store, i.e. the design anticipates more than one API replica.**
  - Revoking `upload` or `novelty` for a role, or demoting an admin, stays unenforced on every other replica for up to 60 s - and require_upload_access / require_novelty_access (dependencies/auth.py:436, :419) read straight through that cache. services/turnstile.py:11-13 documents the same single-process assumption explicitly; the two caches in auth.py do not.
  - `rag-thesis-backend/dependencies/auth.py:324-348, rag-thesis-backend/routers/settings.py:75, rag-thesis-backend/routers/analytics.py:353, rag-thesis-backend/config.py:246-247`

- **`PUT /settings/features` still contains the update-then-insert race that the sibling `GET /settings/features` docstring says was removed. routers/settings.py:67-73 issues `.update(...).eq('key','role_features')` and, when `res.data` is empty, falls through to `.insert(...)` at :74-78. Two concurrent PUTs against a missing row both see an empty update result and both insert; the loser gets a 23505 primary-key violation, which is not caught and surfaces as an unhandled 500. routers/settings.py:47-55 describes exactly this shape ("two concurrent first loads raced on the primary key and surfaced a 500") as the reason the GET path stopped inserting.**
  - Low reachability (superadmin-only, and the row is seeded by supabase_setup.sql:742), but it is the same defect the neighbouring comment asserts is gone, and services/db_errors.is_unique_violation already exists to classify it.
  - `rag-thesis-backend/routers/settings.py:63-78, rag-thesis-backend/routers/settings.py:47-55`

- **Two divergent implementations of the same Postgres error classification, one of which bypasses the module written to prevent exactly that. services/db_errors.py:55-66 `is_missing_column` probes `code`, `pgcode`, and `details`/`message`/`str(error)`; its module docstring says "Keeping the probe in one place stops the call sites from disagreeing." services/retriever.py:476-482 `_is_missing_column_error` is a private re-implementation that checks only `getattr(error,'code')`, the literal substring "'code': '42703'", and `str(error)` - it never reads `pgcode` or `.details`/`.message`, and services/retriever.py does not import db_errors at all (imports at :12-21).**
  - Both gate five separate legacy-column fallbacks in retriever.py (:208, :258, :585, :607, and the archive listing). If an SDK version exposes the SQLSTATE on `pgcode` or only inside `.details`, retriever.py re-raises where every other router falls back - or, in the get_paper_overview_context case, would have fallen back onto the provenance-less legacy query. The divergence is silent in either direction.
  - `rag-thesis-backend/services/retriever.py:476-482, rag-thesis-backend/services/db_errors.py:55-66`

- **`_persist_turn` is a lost-update read-modify-write on `scan_history.chat_log`, and unlike every other write on that table it is not owner-scoped. routers/duplication.py:77-89 takes the `chat_log` list read earlier in the handler, appends two entries, truncates, and writes the whole array back with `.update({'chat_log': updated}).eq('id', scan_id)` - no `user_id` filter. The two delete endpoints on the same table deliberately carry it on both the read and the write, with a comment explaining why: "so a guessed id cannot remove another account's row even if the check above were ever reordered away" (routers/duplication.py:591-606).**
  - Ownership is currently established by the `.eq('user_id', user.id)` read at routers/duplication.py:466-471, so this is not exploitable today - but it is the single write on scan_history that depends on statement ordering rather than its own predicate, in a file that explicitly defends against that reordering elsewhere. Two concurrent follow-ups on one scan also silently drop a turn (rate limit is 20/minute per user, so a double-submit reaches it).
  - `rag-thesis-backend/routers/duplication.py:77-89, rag-thesis-backend/routers/duplication.py:591-606`

- **Unbounded prompt construction from a stored jsonb column. routers/duplication.py:505-512 iterates EVERY entry in `scan.get('matched_chunks')` and concatenates the full uploaded-draft and archive text of each into `pairs_str`, with no slice and no character cap, before embedding it in the follow-up prompt at :527-531. By contrast the conversation history immediately above it is capped at `chat_log[-5:]` with each turn passed through `prompts.fence_untrusted` (which truncates), and `prompts.followup_rewrite_prompt` caps its untrusted turns at 4000/300 chars.**
  - A 500-page manuscript that matches heavily produces hundreds of excerpt pairs; the prompt grows linearly with them and either exceeds the model context (502 to the reviewer on every follow-up for that scan, permanently) or is silently truncated at the provider. Every other untrusted-text path in the codebase bounds its input.
  - `rag-thesis-backend/routers/duplication.py:505-512, rag-thesis-backend/routers/duplication.py:527-531`

- **`BodySizeLimitMiddleware` is registered outside `security_headers`, so the 413 it emits carries none of the OWASP baseline headers. Starlette applies middleware in reverse registration order; the order in main.py is SlowAPI (:155), GZip (:158), security_headers (:161), BodySizeLimitMiddleware (:180), CORS (:187). That makes the outer-to-inner chain CORS -> BodySizeLimit -> security_headers -> GZip -> SlowAPI. `_send_too_large` (services/request_limits.py:80-92) writes its response directly to the outer `send`, so it never passes through security_headers and ships with no X-Content-Type-Options, X-Frame-Options, Referrer-Policy or Permissions-Policy.**
  - Minor alone, but the comment block at main.py:182-186 asserts that registering CORS last means "every response carries its headers - including rate-limit rejections and errors raised inside the inner middleware". That is true for CORS and false for the security headers, on the one response path a caller can trigger without authenticating.
  - `rag-thesis-backend/main.py:155-187, rag-thesis-backend/services/request_limits.py:80-92`
